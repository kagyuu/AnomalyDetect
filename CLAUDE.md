# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## このリポジトリの現状(2026-09-06 時点)

**アプリケーションは完成し、バージョン 0.5.0 である。** CR-001〜009 まで適用済み。
`docs/.inprogress` は存在せず、通常フローは完了している。

### 何を作ったか

**`s_anomaly`** — 運用ログ(DB コネクション数 / JVM GC / LSF キュー)を DuckDB に
取り込み、**11 個の異常検知アルゴリズム**を当てて、Markdown のレポート群と
SVG チャートを出力する単一の Python CLI。

```
python app/s_anomaly.py <解析対象ディレクトリ>
```

| 資産 | 場所 |
| --- | --- |
| エントリポイント | `app/s_anomaly.py` |
| 実装 | `app/src/s_anomaly/`(M01〜M15) |
| テスト | `app/tests/`(単体 / 結合 / 受け入れ) |
| 設定 | `app/settings.properties` |
| **利用者向け説明書** | **`README.md`**(リポジトリ直下に一本化。`app/README.md` は削除済み) |
| 設計の全体像 | `docs/ArchitectureHandbook.md` |
| 設計判断の理由 | `docs/ADR.md`(ADR-001〜020) |

### ビルド・テストのコマンド

```
python -m compileall -q app                                  # ビルド確認
python -m unittest discover -s app/tests/unit -t app         # 単体
python -m unittest discover -s app/tests/integration -t app  # 結合
python -m unittest discover -s app/tests/acceptance -t app   # 受け入れ

# 大規模データでの性能テスト(既定ではスキップ)
S_ANOMALY_RUN_A006=1 python -m unittest discover -s app/tests/acceptance -t app -p "test_a006_performance.py"
```

**件数は変動するため、ここには書かない。** 最新は `BUILD_HISTORY.md` にある。

**テストスイートは続けて 2 回実行し、同じ結果になることを確認する**(後述の規則)。

### 次に改修するときの入口

**まずこの 3 つを読む。**

| # | 文書 | 何が分かるか |
| --- | --- | --- |
| 1 | `INDEX.md` | リポジトリ全体の目次と**現在の状態** |
| 2 | `docs/ArchitectureHandbook.md` | 設計の全体像・モジュールの責務・**既知の制約** |
| 3 | `docs/ADR.md` | **なぜそう作ったか。** 同じ誤りを繰り返さないための記録 |

**改修は CR(変更要求)として起票する。** 手順は `docs/CR.md` の冒頭にある。
過去の 9 件が `docs/P901-cr-direction/` と `docs/P903-cr-records/` にあり、
**書き方の見本になる。**

### 実装を変える前に必ず読む ADR

**この 4 つは、知らずに触ると静かに壊れる。**

| ADR | 内容 |
| --- | --- |
| **ADR-010** | 実行順序 S7 → S7' → S8 → S8'。順序を誤ると原因候補が**無言で発火しなくなる** |
| **ADR-017** | **検知を系列単位の SQL 窓関数へ移さない。** 実装して測ったら純 Python より 1.9 倍遅かった |
| **ADR-018** | S6 の各スレッドは `con.cursor()` を持つ。**共有接続に戻すと動くが 5 分の 1 の並列度になる** |
| **ADR-019** | `ts` は `TIMESTAMP` のまま保つ。`TIMESTAMPTZ` にすると**環境によって出力が変わり再現性を壊す** |

### 繰り返し起きている欠陥の型

**「設定の既定値が、別の機能の前提と噛み合わず、エラーを出さずに機能が消える」**
という欠陥が**すでに 2 回**起きている。

| 回 | 内容 |
| --- | --- |
| CR-002 | 原因候補ルール 4 件が無言で発火しなくなった(観点2 に同時アノマリーが付かないため) |
| CR-009 | 重ね合わせチャートが 1 枚も描かれなかった(`SEVERE` は観点2 であり同時アノマリーを持たないため) |

**機能を足したら「実際にその機能が動いた証跡」を確認するテストを書くこと。**
「例外が出ない」だけでは通ってしまう。

## アーキテクチャ = 仕様駆動開発ワークフロー

**開発の進め方**は `spec-driven-dev` Skill が定義するフェーズ・ワークフローに従う。
全体像は `.claude/skills/spec-driven-dev/SKILL.md`(これが唯一の権威)にある。

**アプリ自体のアーキテクチャは `docs/ArchitectureHandbook.md` にある。** 混同しないこと。

日本のウォーターフォール/V字モデルを模した7ステップ構成で、各フェーズ(P000〜P905の通し番号)が `docs/` 配下に Markdown 成果物を1つ生成する。

| Step | フェーズ | 役割 |
| --- | --- | --- |
| Require Development | P000–P001 | 要求整理・システム要件定義(**唯一の人間対話ステップ**) |
| Plan Loop | P002–P012 | UI設計・詳細設計・トレーサビリティ・実装/テスト計画・実装/テスト定義、そして P010 横断レビュー → P011 影響分析 → P012 修正 のループ |
| Overview | P020–P022 | `INDEX.md` / `docs/ADR.md` / `docs/ArchitectureHandbook.md` を作り、後続 Agent が全資産を読み直さずに済むようにする |
| Executor | P101–P104 | 実装コンテキスト構築・実装+単体テスト・結合テスト実行・INDEX更新 |
| Reviewer Loop | P201–P205 | 実装横断レビュー → 修正計画 → 修正 → 影響分析 → 再テスト のループ |
| Closing | P301–P302 | 全体 `./INDEX.md` 作成・納品物(Docker Compose 等)作成 |
| Refactor | P900–P905 | 変更要求(CR)にもとづく改修。内部で P002〜P302 を再実行する |

### 重要な原則

- **フェーズは番号順に1つずつ実行する。飛ばさない。** 各フェーズは前フェーズの成果物を入力とする。
- **フェーズごとに人間のレビューを待たない。** 処理を止めるのは各Stepに定義された「停止条件」に該当したときだけ(例: P010を3回実行しても設計矛盾が解消しない、P102で3回自己修正しても単体テストが通らない、P201を3回実行しても全テスト合格にならない)。
- 設計書の作成(高性能モデル)と、それにもとづく実装・テストの実行(低性能モデル)を別 Agent に分担させる前提で書かれている。そのため P007/P008/P009 の成果物は「定義書 兼 実行指示書」という形をとる。

## 制御ファイル(`docs/` 直下)

成果物ではなくワークフローの状態を持つファイル。フェーズの進捗判定には `.inprogress` **のみ**を使う。

- `docs/.inprogress` — 現在実行中のフェーズ番号。フェーズ着手時に記入し、**完了時は次に実行すべき番号へ直ちに更新する**(完了したフェーズの番号を残さない)。通常フローの P302 完了時のみ削除。ただし P903 の内側で P302 が完了した場合は削除せず `P904` に更新する。Refactor 実行中は `P903:P006` のような入れ子表記になり、この表記があれば台帳より優先して CR 文脈のまま内側の P番号から再開する。
- `docs/.mode` — 実行モード。`一気通貫`(既定。P002〜P302 を人間のゲートなしで走り切る)または `工程確認`(Step境界の最大6箇所で止まり、そのStepの★FIXME★を人間に解消してもらう)。ファイルが無ければ `一気通貫` として扱う。
- `docs/.stop-report.md` — 停止条件に該当して人間に報告した内容の記録。会話上の報告だけで済ませない。
- `docs/CR.md` — CR状態台帳(`未対応`/`対応中`/`反映確認中`/`完了`/`却下`)。`.inprogress` が存在しない場合にのみ再開判定の材料に使う。

### セッション再開時の判定(ステップ0)

セッションが切れても再開できるよう、`SKILL.md` の「ステップ0」に機械的な判定手順がある。**必ず `docs/.inprogress` の有無と内容を最初に確認する。** 存在しなければ `docs/P001-requirement.md` 〜 `docs/P302-deliver.md` と `./INDEX.md` の有無から再開先を特定し、全部揃っていても直ちに完了と判定せず `docs/CR.md` に未完了CRが残っていないか、直近CRに P905 の判定記録があるかを必ず確認する。

## 記法上の規約

- **★FIXME★** — Agent が自分の想定で補った、後で人間の確認が必要な箇所の目印。**未解決**を意味する。必ず対象の記述と同じ行末または直後の行に置き、文書末尾にまとめない。これは処理を止める合図ではない。
- **★ACCEPTED★** — 検討のうえ意識的に選んだ制約・割り切り。**解決済み**を意味する。「何を検討したか」「なぜ採らなかったか」「残存リスク」を必ず併記する。理由なしに記号だけを置かない。★ACCEPTED★ が付いた割り切りをレビューで蒸し返さない(ただし他文書との整合性チェックの対象からは外れない)。
- **OKF形式**(P007/P008/P009/P202 の目次) — `- [状態] 番号 [タイトル](相対リンク) — 一言概要`。状態は `[ ]` 未着手 / `[~]` 進行中 / `[x]` 完了。これらのフェーズは目次の全項目が `[x]` になって初めて完了とみなす。完了済み項目に追加作業が必要になったら `[~]` に戻して理由を注記する。子ドキュメントを移動した場合はリンク先も同時に書き換える。
- **INDEX形式**(P020/P104/P301) — 進捗の意味を持たない、パス + 一言概要の単純な目次。
- 図は Mermaid 記法で書く。図そのものの完成度は評価しないが、図と本文の内容が矛盾していないかは P010 のレビュー対象になる。

## 生成物の扱い(2026-09-06 に人間が指示)

**このアプリ自体のエンハンス作業でレポートやチャートを生成した場合は、
作業の最後に `docs/report-samples/` へ移動する。**

* 対象は `report_*.md` と `charts/`(SVG)である。
* **リポジトリ直下や `app/` 配下に置いたままにしない。**
* A006(性能テスト)は `S_ANOMALY_A006_KEEP` を指定すると自動で複写する。
* **`docs/report-samples/` の中身と `app/tests/_work/` は `.gitignore` で除外している**
  (いずれも生成物。前者は 100MB 超、後者は `_setup_baseline.restore()` で再生成できる)。
  **移動はするが、コミットはされない。**

```
S_ANOMALY_A006_KEEP=./docs/report-samples S_ANOMALY_RUN_A006=1   python -m unittest discover -s app/tests/acceptance -t app -p "test_a006_performance.py"
```

* **手で動かした確認は、原則スクラッチパッドで行う**(そもそもリポジトリに出さない)。
  残す価値のある出力だけを `docs/report-samples/` へ移す。

### ★注意★ `cli.run_pipeline` は `os.getcwd()` を出力先にする

**同一プロセスで `cli.main()` を呼ぶテストは、必ず一時ディレクトリへ
`os.chdir()` してから呼ぶこと。** 移らないと**リポジトリ直下へレポートと
`charts/` を書いてしまう**(2026-09-06 に `test_cli_args.py` /
`test_cli_ingest.py` で実際に発生した)。
回帰試験は A011 `test_07a_test_suites_do_not_pollute_the_repository` にある。

## テスト・ビルドを扱うときの規則

コードが生成された後、この規則が効いてくる。

- **実行していないコマンドを、実行済み・動作確認済みであるかのようにドキュメントへ書かない。** テスト実行コマンドを文書に記載する前に一度実際に流し、パス指定の誤りで0件が静かにスキップされていないかを確認する。
- **テストスイートは続けて2回実行して同じ結果になることを確認する。** 永続化データストアを共有するテストは、前回実行分のデータが残って2回目だけ落ちることがある。2回目が失敗する場合はアプリではなくテスト側の欠陥として扱う。この確認は P103・P201・P203・P205 が行う。
- **テスト実行やビルドが結果を出力しないまま異常終了した場合(ネイティブクラッシュ、無出力終了、異常な終了コード)は、まず実行環境・ツールチェーン側を疑う。** 「テストが失敗した」のではなく「テストを実行できなかった」状態である。対象コードを変更して様子を見る進め方をせず、**最小再現コードを書いて切り分けてから**判断する。環境要因と判明したら `docs/ArchitectureHandbook.md` の「既知の制約・技術的負債」に記録する(P101 の入力なので後続 Executor に引き継がれる)。切り分けなしに環境要因と断定しない。
- 停止条件に該当して人間に報告するときは、問題だけでなく**対処案を提示する**。対処案を組み立てられない場合(判断材料が人間側にしかない等)は「この問題に対して人間の判断が必要ですが、Claude Code は対処案を提示できません。その理由は{具体的な理由}です。」と理由を明記する。提示は「提示」であって「実施」ではない — 各フェーズの禁止事項(P302は仕様変更をしない、P103は失敗テストをその場で修正しない、P205は期待値を改ざんしない)を、対処案を出せることを理由に越えない。
