# --------------------------------------------------------------------------
# Python
# --------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/

# --------------------------------------------------------------------------
# テストデータ (生成物)
#
# `tests/integration/_setup_baseline.py` の restore() でいつでも再生成できる。
# 約 2.4MB あり、内容はすべて gen_testdata.py が作る合成データである。
# --------------------------------------------------------------------------
app/tests/_work/

# --------------------------------------------------------------------------
# レポートのサンプル (生成物)
#
# A006 を `S_ANOMALY_A006_KEEP` 付きで実行すると再生成される (README は残す)。
# 100MB を超えるため、リポジトリには含めない。
# 生成方法は docs/report-samples/README.md にある。
# --------------------------------------------------------------------------
docs/report-samples/*
!docs/report-samples/README.md

# --------------------------------------------------------------------------
# アプリの実行結果
#
# カレントディレクトリに出力されるため、リポジトリ直下で実行すると残る。
# ※本来テストは一時ディレクトリで実行する (CLAUDE.md「生成物の扱い」)。
# --------------------------------------------------------------------------
report_*.md
charts/
tmp/

# --------------------------------------------------------------------------
# 閉域環境向けの DuckDB wheel
#
# 配布先の OS・アーキテクチャ・Python 版ごとに用意するバイナリであり、
# リポジトリには含めない。準備手順は README.md 6 章にある。
# --------------------------------------------------------------------------
app/vendor/*
!app/vendor/.gitkeep

# --------------------------------------------------------------------------
# OS / エディタ
# --------------------------------------------------------------------------
.DS_Store
Thumbs.db
*.swp
