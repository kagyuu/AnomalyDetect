# Build History

本リポジトリは **git 管理されていない**。したがって Commit 列はすべて `未取得` である。
代わりに、作業前の退避物(zip)のファイル名を Notes 欄に記して変更時点を辿れるようにしている。

「Build」とは、本アプリの場合 `python -m compileall -q app`(全モジュールのバイトコンパイル)を指す。
配布物はソースそのものであり、バイナリを生成するビルド工程は存在しない(NFR-011「配布資産をコピーするだけで動作すること」)。

| Build ID | Version | Date/Time | Commit | Build Command | Test Command | Result | Notes |
|---|---|---|---|---|---|---|---|
| B-001 | 0.1.0 | 2026-09-02 05:21 | 未取得 | `python -m compileall -q app` | `python -m unittest discover -s app/tests/unit -t app` / `... integration ...` | PASS | P103(結合テスト実行)。記録: `docs/test-records/20260902-0521-test-record.md` |
| B-002 | 0.1.0 | 2026-09-03 00:10 | 未取得 | `python -m compileall -q app` | 上記 + `... acceptance ...` | **FAIL** | P201(実装横断レビュー 1 回目)。**A006 が FAIL**(ALG-B4 の全走査により大規模データで完了せず)。記録: `docs/test-records/20260903-0010-test-record.md` |
| B-003 | 0.1.0 | 2026-09-04 03:35 | 未取得 | `python -m compileall -q app` | 上記 + `S_ANOMALY_RUN_A006=1 ... test_a006_performance.py` | **FAIL** | P203(F001/F002)適用後の P205 再判定。単体 376 / 結合 105 / 受け入れ 109 は全件 PASS。**A006 は依然 FAIL**(S8 が既定の 4GB で OOM)。退避物: `s_anomaly-20260903004204.zip`(F001 適用前)。記録: `docs/test-records/20260904-0335-test-record.md` |
| B-004 | 0.1.0 | 2026-09-04 05:30 | 未取得 | `python -m compileall -q app` | 上記 | **PASS** | P203(F003)適用後の P205 再判定。**単体 376 / 結合 105 / 受け入れ 122(A006 の 13 件を含む)が全件 PASS**。退避物: `s_anomaly-20260904034940.zip`(F003 適用前)。記録: `docs/test-records/20260904-0530-test-record.md` |
| B-005 | **0.2.0** | 2026-09-05 06:05 | 未取得 | `python -m compileall -q app` | 上記 + `S_ANOMALY_RUN_A006=1 ... test_a006_performance.py` | **PASS** | **CR-001〜004 対応**(レポートのホスト別分割・同時アノマリー・イベント ID・集計サマリ)。**単体 410 / 結合 108 / 受け入れ 125 が全件 PASS**。退避物: `s_anomaly-20260905031746.zip`(CR 適用前)。記録: `docs/test-records/20260905-0530-test-record.md` |
| B-006 | **0.3.0** | 2026-09-05 15:30 | 未取得 | `python -m compileall -q app` | 上記 | **PASS** | **CR-005〜007 対応**(上限への張り付き検知・系列単位のキャッシュ・S6 のスレッド並列)。**単体 426 / 結合 113 / 受け入れ 131 が全件 PASS**。退避物: `s_anomaly-20260905142048.zip`(CR 適用前)。記録: `docs/test-records/20260905-1530-test-record.md` |
| B-007 | **0.4.0** | 2026-09-06 02:40 | 未取得 | `python -m compileall -q app` | 上記 | **PASS** | **CR-008 対応**(入力種別ごとのタイムゾーン設定・投入時の変換・レポートへの併記)。**単体 451 / 結合 115 / 受け入れ 143 が全件 PASS**。退避物: `s_anomaly-20260906001144.zip`(CR 適用前)。記録: `docs/test-records/20260906-0240-test-record.md` |
| B-008 | **0.5.0** | 2026-09-06 04:00 | 未取得 | `python -m compileall -q app` | 上記 | **PASS** | **CR-009 対応**(アノマリーの状況を SVG の折れ線チャートで出力、同時アノマリーの重ね合わせ)。**単体 495 / 結合 115 / 受け入れ 159 が全件 PASS**。退避物: `s_anomaly-20260906031227.zip`(CR 適用前)。記録: `docs/test-records/20260906-0400-test-record.md` |

## 各ビルドの検証内容(B-008)

**現時点の最新ビルドである。** CLAUDE.md の規則に従い、各スイートを**連続 2 回**実行している。

| 対象 | 1 回目 | 2 回目 |
| --- | --- | --- |
| ビルド `python -m compileall -q app` | 終了コード 0 | — |
| 単体テスト | `Ran 495 tests in 224.581s` / `OK (skipped=1)` | `Ran 495 tests in 225.072s` / `OK (skipped=1)` |
| 結合テスト | `Ran 115 tests in 280.026s` / `OK` | `Ran 115 tests in 289.772s` / `OK` |
| 受け入れテスト(A006 を除く) | `Ran 159 tests in 385.701s` / `OK (skipped=19)` | `Ran 159 tests in 377.217s` / `OK (skipped=19)` |
| 受け入れテスト A006 | `Ran 19 tests in 425.405s` / `OK` | `Ran 19 tests in 396.301s` / `OK` |

### 性能とチャートの出力量

| 規模 | B-007(0.4.0) | **B-008(0.5.0)** | チャート |
| --- | --- | --- | --- |
| 通常 30 日(950,400 行) | 53.1s | **95.3s** | **2,430 枚 / 17.8MB** |
| 大規模 90 日(2,851,200 行) | 162.8s | **302.4s** | 2,430 枚 |

**チャートの追加により所要が 30 日で +80%、90 日で +75% 増えた。**
枚数は `[chart]` の上限(ホストあたり **100 + 100**)で抑えられており、
上限が無ければイベント約 7,200 件ぶんが出力される。
**NFR-002(600 秒)には収まっている**(90 日で 302 秒 / 余裕 50%)。

| 上限の設定 | 枚数 | 合計サイズ | 30 日の実行時間 |
| --- | --- | --- | --- |
| 10 + 10 | 340 枚 | 3.7MB | 55.3s |
| **100 + 100(既定)** | **2,430 枚** | **17.8MB** | **95.3s** |

### 各ビルドの検証内容(B-007 / 旧)

| 対象 | 1 回目 | 2 回目 |
| --- | --- | --- |
| 単体テスト | `Ran 451 tests in 256.026s` / `OK (skipped=1)` | `Ran 451 tests in 247.659s` / `OK (skipped=1)` |
| 結合テスト | `Ran 115 tests in 365.331s` / `OK` | `Ran 115 tests in 583.200s` / `OK` |
| 受け入れテスト(A006 を除く) | `Ran 143 tests in 547.232s` / `OK (skipped=18)` | `Ran 143 tests in 396.339s` / `OK (skipped=18)` |
| 受け入れテスト A006 | `Ran 18 tests in 232.951s` / `OK` | `Ran 18 tests in 232.400s` / `OK` |

**タイムゾーンの追加による性能への影響は無い。** 既定はすべて UTC であり、
**変換の SQL が一度も発行されない**ためである(`docs/ADR.md` ADR-019)。

| 規模 | B-006(0.3.0) | **B-007(0.4.0)** |
| --- | --- | --- |
| 通常 30 日(950,400 行) | 55.1s | **53.1s** |
| 大規模 90 日(2,851,200 行) | 173.6s | **162.8s** |

### 各ビルドの検証内容(B-006 / 旧)

| 対象 | 1 回目 | 2 回目 |
| --- | --- | --- |
| 単体テスト | `Ran 426 tests in 261.597s` / `OK (skipped=1)` | `Ran 426 tests in 261.203s` / `OK (skipped=1)` |
| 結合テスト | `Ran 113 tests in 293.398s` / `OK` | `Ran 113 tests in 291.630s` / `OK` |
| 受け入れテスト(A006 を除く) | `Ran 131 tests in 248.827s` / `OK (skipped=18)` | `Ran 131 tests in 250.070s` / `OK (skipped=18)` |
| 受け入れテスト A006 | `Ran 18 tests in 247.801s` / `OK` | `Ran 18 tests in 247.013s` / `OK` |

`skipped=18` は A006 の 2 クラス(大規模 12 件 + 通常 6 件)であり、`S_ANOMALY_RUN_A006=1` を付けて別途実行している。

### 性能(既定の `max_memory = 4GB`)

| 規模 | B-004(0.1.0) | B-005(0.2.0) | **B-006(0.3.0)** | うち S6 |
| --- | --- | --- | --- | --- |
| 通常 30 日(950,400 行) | 147.4s | 69.9s | **55.1s** | **23.9s** |
| 大規模 90 日(2,851,200 行) | 531.3s | 230.9s | **173.6s** | **68.6s** |

**B-006 の改善の主因は S6(検知)である**(48.0s → 23.9s。-51%)。
**同じ系列のデータを検知器の数だけ読み直していた**のを止めた(`docs/ADR.md` ADR-017)。
アルゴリズムを 1 つ増やした(ALG-C1)うえでの数値である。

**支配ステップは S6 から S4(取り込み)へ移った**(S6 が全体に占める割合 43%)。

### 各ビルドの検証内容(B-005 / 旧)

| 対象 | 1 回目 | 2 回目 |
| --- | --- | --- |
| 単体テスト | `Ran 410 tests in 201.010s` / `OK (skipped=1)` | `Ran 410 tests in 213.203s` / `OK (skipped=1)` |
| 結合テスト | `Ran 108 tests in 261.665s` / `OK` | `Ran 108 tests in 274.864s` / `OK` |
| 受け入れテスト(A006 を除く) | `Ran 125 tests in 271.343s` / `OK (skipped=15)` | `Ran 125 tests in 273.744s` / `OK (skipped=15)` |
| 受け入れテスト A006 | `Ran 15 tests in 319.101s` / `OK` | `Ran 15 tests in 321.560s` / `OK` |

**CR-002 により S8(相関の算出)が `metrics` を参照しなくなったことが主因であった。**

## 実行環境

| 項目 | 値 |
| --- | --- |
| OS | Windows 11 Pro 10.0.26200 |
| Python | 3.14 |
| DuckDB | 環境インストール版(`vendor/` 経路は未実証) |
| 物理メモリ | 51.2GB |

**Linux(WSL2 Ubuntu / Python 3.10)での実行は A005(両 OS 一致)が確認している。**

## バージョンの管理方法

* バージョン番号は **`app/VERSION`**(現在 `0.5.0`)に置く。
* 実行時は `s_anomaly.cli.read_version()` が `app/VERSION` を読み、**起動時の進捗ログに出力する**。

  ```
  2026-09-06 04:00:00 [INFO] [--] s_anomaly 0.5.0 を起動しました (Python 3.14.2, DuckDB 1.5.5)
  ```

* **`--version` オプションは持たない。** 引数は解析対象ディレクトリの 1 つだけであり(`docs/P002-frontend-spec.md` UI-01)、オプションの追加は仕様変更にあたるため P302 では行わない。
* 更新方針: バグ修正のみは PATCH、後方互換の機能追加は MINOR、契約を壊す変更は MAJOR。**ただし MAJOR が 0 の間は、破壊的変更であっても MINOR を上げる。** `1.0.0` への移行は人間の明示の指示があった場合にのみ行う。
