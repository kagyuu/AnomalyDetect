# テスト記録 — 20260903-0010 (P201 受け入れ結合テスト実行)

* 実行フェーズ: P201(実装横断レビュー、P009 実行分)
* 対象: `docs/P009-acceptance-direction.md` の A001〜A011
* 形式: `.claude/skills/spec-driven-dev/TEMPLATE-test-record.md` に従う

## 事前ビルド結果 (全テスト共通)

```
python -m compileall -q app                                                  -> 終了コード 0
python -c "import sys; sys.path.insert(0,'app/src'); import s_anomaly.cli"   -> 終了コード 0
```

構文エラー・import 時エラーなし。**ビルド成功のため全テストへ進んだ。**
`docs/P008-test-direction.md` の T001〜T008 はすべて `[x]` かつ PASS であり、
A001 以降の前提条件を満たしている。

## ベースライン (P006 TP-03 〜 TP-06)

スイート開始時に 1 回だけ実行した。A004 の 1 回目と 2 回目の間には実行していない。

```
python app/tests/integration/_setup_baseline.py
```

## テストハーネス

`docs/P009-acceptance-direction.md` 4.2 に従い、**すべて `subprocess` でアプリを
起動して外側から観測した**。`main()` を直接呼ぶ形は使っていない。
共通処理は `app/tests/acceptance/_harness.py` に置いた。

## サマリ

| 結果 | 件数 |
| --- | --- |
| PASS | 10 (A001〜A005, A007〜A011) |
| **FAIL** | **1 (A006)** |
| BLOCKED | 0 |
| NOT RUN | 0 |

**A006 が FAIL であるため、P202(修正計画)への引き渡しが必要である。**

| テストID | 実行コマンド (`-p` の値) | 件数 | 所要 | 結果 |
| --- | --- | --- | --- | --- |
| A001 | `test_a001_normal_run.py` | 15 | 21.0s | PASS |
| A002 | `test_a002_exit_codes.py` | 14 | 86.3s | PASS |
| A003 | `test_a003_reproducibility.py` | 10 | 64.8s | PASS |
| A004 | `test_a004_restart.py` | 9 | 64.5s | PASS |
| A005 | `test_a005_cross_os.py` | 12 | 92.8s | PASS |
| A006 | `test_a006_performance.py` | 9 | (中断) | **FAIL** |
| A007 | `test_a007_memory_spill.py` | 9 | 137.2s | PASS |
| A008 | `test_a008_no_network.py` | 9 | 48.4s | PASS |
| A009 | `test_a009_broken_input.py` | 12 | 2.5s | PASS |
| A010 | `test_a010_portability.py` | 10 | 157.8s | PASS |
| A011 | `test_a011_user_privilege.py` | 9 | 52.8s | PASS |

いずれも次の形式で実行し、**0 件で静かに成功していないこと(件数が出ること)を
確認した**(`docs/P009-acceptance-direction.md` 5章 ★FIXME★ の確認)。

```
python -m unittest discover -s app/tests/acceptance -t app -p "{ファイル名}" -v
```

---

## A001: 正常系の通し実行と正解突き合わせ

- 実行日時: 2026-09-02 05:41
- 実行者: Reviewer (P201)
- 対象ブランチ/コミット: (git 未初期化のため該当なし)
- 参照したテスト指示: docs/P009-acceptance-direction/A001-normal-run.md
- 実行環境: Windows 11 Pro / Python 3.14.2 / DuckDB 1.5.5
- 事前ビルド結果: 成功
- 実行コマンド: `python -m unittest discover -s app/tests/acceptance -t app -p "test_a001_normal_run.py" -v`
- 結果: **PASS** (15 tests / 21.0s)
- 期待結果: A001【期待結果】の 14 項目
- 実際の結果: 全 14 項目が期待値と一致した。
  * 終了コード 0、`report.md` 生成、`report.md.tmp` なし、`Traceback` なし
  * 4 章 + 3 小節がすべて存在。1 章に対象期間 2026-06-01 00:00:00 〜 2026-06-14 23:55:00
  * 2 章に ALG-A1〜A5 / ALG-B1〜B5 の 10 個すべて
  * **検知漏れ 0**(`injected` 7 件すべてが、期待アルゴリズムを含むイベントとして前後 60 分の許容内で存在)
  * **`clean_series` の SEVERE 0 件**
  * 脅威度が全 `injected` で `expect_min_severity` 以上
  * 全イベントに 10 項目が漏れなく存在
  * BOM なし UTF-8 / 改行 LF、標準出力に `[S1]`〜`[S9]`
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- テスト指示との矛盾の疑い: **あり(上位文書との矛盾)**。A001【期待結果】項目 10 は「誤検知: `clean_series` に属するイベントが **0 件**」としているが、**A001【実行手順】5 が突き合わせ基準として参照を指示している `docs/P002-frontend-spec.md` UI-08-03 は、ADR-012 にもとづき「`clean_series` に属する検知点が総点数の 1% 未満、かつ SEVERE のイベントが 0 件」へ改められている**。A001 の指示は P010 第 2 回レビュー以前の文言が残ったものと判断し、**現行の仕様書である UI-08-03 を判定基準として採用した**。両方の実測値を記録する: SEVERE 0 件(UI-08-03 の基準を満たす)、`clean_series` に属するイベントは存在する(A001 の旧文言では不合格)。統計的手法である以上、4,000 点の系列に 2σ 判定を当てれば正規ノイズだけで約 180 点が偶然閾値を超えるため、旧文言の「0 件」は達成不能である
- 次工程への申し送り: 上記の矛盾について、**A001 の期待結果表を UI-08-03 に合わせて改める**ことを P202 で検討されたい(アプリの欠陥ではなく、テスト指示側の追随漏れである)

## A002: 終了コード 9 種の再現

- 実行日時: 2026-09-02 05:45
- 実行者: Reviewer (P201)
- 参照したテスト指示: docs/P009-acceptance-direction/A002-exit-codes.md
- 実行環境: Windows 11 Pro / Python 3.14.2 / DuckDB 1.5.5
- 事前ビルド結果: 成功
- 実行コマンド: `python -m unittest discover -s app/tests/acceptance -t app -p "test_a002_exit_codes.py" -v`
- 結果: **PASS** (14 tests / 86.3s。`NOT RUN` とした項目なし)
- 期待結果: A002【期待結果】の 13 項目
- 実際の結果: 13 項目すべてが期待値と一致した。**9 種すべてをプロセスの終了ステータスとして再現できた。**

  | # | 内容 | 期待 | 実際 |
  | --- | --- | --- | --- |
  | 1 | 正常終了 | 0 | 0 |
  | 2 | 引数 0 個 | 1 + usage | 一致 |
  | 3 | 引数 2 個 | 1 +「引数は 1 個です」+ 受け取った引数 | 一致 |
  | 4 | 存在しないパス | 1 +「存在しません」+ パス | 一致 |
  | 5 | ファイル指定 | 1 +「ディレクトリではありません」 | 一致 |
  | 6 | 対象 0 件 | 2 + 探索パス + 3 パターン | 一致 |
  | 7 | 設定不正 | 3 + セクション・キー・値・理由 | 一致 |
  | 8 | 全解析失敗 | 4 + ファイルごとの理由 | 一致 |
  | 9 | 書き込み失敗 | 5 + **標準出力へレポート全文** | 一致 |
  | 10 | メモリ不足 | 6 | 一致(`max_memory=1KB` で再現) |
  | 11 | vendor 不一致 | 7 + 4 要素 | 一致 |
  | 12 | 想定外の例外 | 99 + Traceback | 一致 |
  | 13 | 全アルゴリズム false | 0 + WARNING + 「検知された異常はありません。」 | 一致 |

- 失敗ログ抜粋: なし
- 再現手順: 各終了コードの再現方法は `app/tests/acceptance/test_a002_exit_codes.py` の docstring に記載
- 影響がありそうなモジュール: なし
- テスト指示との矛盾の疑い: **あり(テスト指示側の問題。2 件)**
  1. **設定ファイルの置き場所**。A002【使用するテストデータ】は終了コード 3 / 6 について「正常なログ + `settings.properties`」と書いており、作業ディレクトリに置けば効くように読める。しかし `docs/P002-frontend-spec.md` **UI-02-L01 は「`s_anomaly.py` と同じディレクトリの `settings.properties` を読む」と定めており、実装もそのとおりである**。UI-02-L01 を正とし、`app/` を一時ディレクトリへ複製して複製側の設定を差し替える方式で再現した
  2. **終了コード 7 の再現方法**。A002【事前準備】5 の ★FIXME★ が予告したとおり、**`vendor/{tag}/duckdb/__init__.py` に `raise ImportError` を置くだけでは再現しない**。`bootstrap.load_duckdb` は vendor の読み込みに失敗すると `sys.path` を元に戻して**環境の DuckDB へフォールバックする**設計であり(`docs/P003-backend-spec.md` DS-14-02)、これは意図された正しい挙動である。そこで `PYTHONPATH` に壊れた `duckdb` を置いて環境側も隠したうえで再現した。**あわせて「壊れた vendor だけでは終了コード 0 のまま(フォールバックが働く)」ことも別テストで確認している**
- 次工程への申し送り: 上記 2 件はいずれも**テスト指示の記述の問題であり、アプリの欠陥ではない**。P202 で A002 の記述を実装に合わせて改めることを検討されたい

## A003: 再現性

- 実行日時: 2026-09-02 05:48
- 実行者: Reviewer (P201)
- 参照したテスト指示: docs/P009-acceptance-direction/A003-reproducibility.md
- 実行環境: Windows 11 Pro / Python 3.14.2 / DuckDB 1.5.5
- 事前ビルド結果: 成功
- 実行コマンド: `python -m unittest discover -s app/tests/acceptance -t app -p "test_a003_reproducibility.py" -v`
- 結果: **PASS** (10 tests / 64.8s)
- 期待結果: A003【期待結果】の 9 項目
- 実際の結果: 9 項目すべてが期待値と一致した。
  * 3 回の実行がすべて終了コード 0
  * **`report.md` は「実行日時」の行を除いてバイト単位で完全一致**(3 回とも)
  * イベント ID・並び・脅威度別内訳・2 章のパラメータ・4 章の注意事項がすべて一致
  * 標準出力も一致
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- テスト指示との矛盾の疑い: **なし。ただし比較からの除外を 1 つ追加したため記録する。** 標準出力の S9 行には出力先の絶対パスが出る。A003【事前準備】2 が「1 回目用と 2 回目用に**別々の**一時ディレクトリを作る」ことを指示している以上、このパスは必ず異なる。**アプリの非決定性ではなく指示の帰結である**ため、標準出力の比較でのみ作業ディレクトリのパスを `{WORK}` に正規化した。**`report.md` 側は除外を追加していない**(「実行日時」のみの除外で完全一致した)
- 次工程への申し送り: なし

## A004: 再起動耐性

- 実行日時: 2026-09-02 05:49
- 実行者: Reviewer (P201)
- 参照したテスト指示: docs/P009-acceptance-direction/A004-restart-resilience.md
- 実行環境: Windows 11 Pro / Python 3.14.2 / DuckDB 1.5.5
- 事前ビルド結果: 成功
- 実行コマンド: `python -m unittest discover -s app/tests/acceptance -t app -p "test_a004_restart.py" -v`
- 結果: **PASS** (9 tests / 64.5s)
- 期待結果: A004【期待結果】の 9 項目
- 実際の結果: 9 項目すべてが期待値と一致した。
  * 1 回目・2 回目・3 回目とも終了コード 0。**「2 回目だけ失敗する」事象は起きていない**
  * 2 回目・3 回目の前に `report.md.tmp`(壊れた内容)と `tmp/stale.tmp` を意図的に置いたが、いずれも実行後に `report.md.tmp` は残っていない
  * 3 回の `report.md` が実行日時の行を除いて完全一致。イベント ID・脅威度別内訳も一致
  * 2 回目の標準エラーに `Traceback` / `already exists` / `duplicate` が出ていない(スキーマ適用の冪等性)
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- テスト指示との矛盾の疑い: **あり(A002 と同根)**。A004【事前準備】3 は「作業ディレクトリに `settings.properties` を置き `[duckdb] temp_directory = ./tmp` を明示する」としているが、UI-02-L01 により設定は `s_anomaly.py` と同じディレクトリから読まれる。**`temp_directory` の既定値がそもそも `./tmp`(作業ディレクトリ相対)であるため、既定のまま起動することで指示の意図(残骸の確認対象を作業ディレクトリ内に閉じる)は満たされている**
- 次工程への申し送り: なし

## A005: 両 OS での一致

- 実行日時: 2026-09-02 05:52
- 実行者: Reviewer (P201)
- 参照したテスト指示: docs/P009-acceptance-direction/A005-cross-os.md
- 実行環境:
  * Windows 11 Pro / **Python 3.14.2** / DuckDB 1.5.5
  * WSL2 Ubuntu / **Python 3.10.12** / DuckDB 1.5.5
- 事前ビルド結果: 両 OS で成功(Linux 側は `python3 -m compileall -q app/src app/tools app/s_anomaly.py` を終了コード 0 で確認)
- 実行コマンド: `python -m unittest discover -s app/tests/acceptance -t app -p "test_a005_cross_os.py" -v`
  * Linux 側の実体: `wsl -d Ubuntu -- bash -lc "cd /mnt/c/app/AnomalyDetect && python3 app/s_anomaly.py ..."`(リポジトリの絶対パスから WSL パスを算出しており、位置が変わっても追随する)
- 結果: **PASS** (12 tests / 92.8s)
- 期待結果: A005【期待結果】の 10 項目
- 実際の結果: 10 項目すべてが期待値と一致した。
  * 両 OS で終了コード 0
  * **`gen_testdata.py` の出力が 56 ファイルすべてバイト単位で完全一致**(改行は両方 LF)
  * **`report.md` が実行日時・対象ディレクトリの行を除いて完全一致**
  * 脅威度別内訳・イベント ID が一致
  * 両方とも改行 LF、Windows 側に BOM なし
  * `SyntaxError` なし = **`docs/P003-backend-spec.md` DS-00-01 の言語バージョン制約(3.9 で動く構文のみ)が守られている**
  * 日本語の文字化けなし(`�` を含まない)
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- テスト指示との矛盾の疑い: なし。「対象ディレクトリ」欄は OS でパス表記が異なるため比較から除外した(A005【修正禁止事項】が明示的に許可している除外である)。除外はこの 1 行と「実行日時」のみである
- 次工程への申し送り: 検証に用いた Linux は **WSL2 上の Ubuntu(Python 3.10.12)**であり、実運用の配布先 Linux とはディストリビューション・Python 版が異なる可能性がある(`docs/P009-acceptance-direction.md` 6章 #1)。**P302 の「未整備事項・人間による確認事項」へ引き継ぐ**

## A006: 大規模データでの完走と実行時間

- 実行日時: 2026-09-02 05:56 開始 / 2026-09-02 23:56 中断
- 実行者: Reviewer (P201)
- 参照したテスト指示: docs/P009-acceptance-direction/A006-performance.md
- 実行環境: Windows 11 Pro / Python 3.14.2 / DuckDB 1.5.5
- 事前ビルド結果: 成功
- 実行コマンド: `python -m unittest discover -s app/tests/acceptance -t app -p "test_a006_performance.py" -v`
- 結果: **FAIL**
- 期待結果: A006【期待結果】の 7 項目。とくに項目 3「**1,000 万レコードで 10 分以内**」(NFR-002)
- 実際の結果:

  **実施した規模(A006 が「必ず記録する」ことを求めている値)**

  | 項目 | 値 |
  | --- | --- |
  | 生成方法 | **(a) `gen_testdata.py` の生成関数を Python から直接呼び、モジュール定数を差し替え**(`gen_testdata.py` 自体は変更していない) |
  | 期間 / 間隔 | 365 日 / 5 分 |
  | 系列数 | 84 (DB 10 ホスト × 6 データソース + **JVM 2** + LSF 12)。**JVM は生成ツールが 2 つに固定しており、指定した 24 は効いていない** |
  | **総レコード数** | ★訂正あり★ 当初 10,091,520 件と記載したが誤り。**実際はテーブル行 7,778,880 行 / ファイル上のデータ行 6,622,560 行**。詳細は本節末尾の訂正を参照 |
  | ファイル数 | 1,460(NFR-001 の上限 1 万以内) |
  | データサイズ | 336 MB |
  | 生成所要時間 | 約 30 秒 |
  | `settings.properties` | `[duckdb] max_memory = 4GB`, `temp_directory = ./tmp` |

  **実行結果**

  | 項目 | 値 |
  | --- | --- |
  | **CPU 時間(ユーザ)** | **101.26 分** |
  | CPU 時間(カーネル) | 0.86 分 |
  | 経過(壁時計) | 1,079.9 分 — **測定値として使えない**(下記参照) |
  | ピーク時のメモリ | 約 2.7 GB(`max_memory = 4GB` の範囲内) |
  | `temp_directory` | `tmp/` に DuckDB の退避ファイル 6 個が作られていた(いずれも 0 バイト) |
  | 終了コード | **取得できず(未完了のまま中断)** |
  | `report.md` | **生成されていない**(S9 に到達していない) |

  | # | 確認項目 | 期待値 | 実際 |
  | --- | --- | --- | --- |
  | 1 | 終了コード | 0(完走) | **未確認(中断)** |
  | 2 | 総レコード数の一致 | 一致 | **未確認(中断)** |
  | 3 | **実行時間** | **10 分以内** | **CPU 101.3 分でも未完了。10 倍以上の超過** |
  | 4 | `report.md` の 4 章構成 | 保つ | **未確認(中断)** |
  | 5 | S4 の進捗 | 定期的に出る | **未確認**(`capture_output` により標準出力がパイプに滞留し、中断時に失われた) |
  | 6 | `MemoryError` / `Traceback` | 含まない | **未確認(中断)** |
  | 7 | 一時ファイル | 次回実行を妨げない | **未確認(中断)** |

  **判定**: A006【合否判定基準】の「CONDITIONAL FAIL(項目 3 のみ未達)」は、項目 1・2・4〜7 が合格であることを条件とする。本件は**項目 1(完走)自体が未確認**であるため、その区分には当たらない。**FAIL とする。**

- 失敗ログ抜粋:

  ```
  停止直前のプロセス情報 (Win32_Process, PID 8928)
    CommandLine : python.exe {tmp}\dist\app\s_anomaly.py {tmp}\large\logs
    起動        : 2026-09-02 05:56:41
    停止        : 2026-09-02 23:56:37
    CPU(user)   : 101.26 分
    CPU(kernel) : 0.86 分
    メモリ      : 2,686 MB / スレッド 31
    直近 20 秒の CPU 消費: 20.4 秒 (= 1 コアを専有して継続稼働。ハングではない)
  作業ディレクトリ: report.md なし / tmp/ に duckdb_temp_storage_*.tmp が 6 個 (0 バイト)
  ```

  停止直前も 1 コアを専有し続けており、**ハングではなく計算を継続していた**。
  スレッドは 31 本あるが CPU 消費は 1 コア分であり、**DuckDB の並列処理ではなく
  Python 側の単一スレッド処理(S6 検知ループ)で時間を消費している**と判断した。

- 再現手順:
  1. `python app/tests/integration/_setup_baseline.py`
  2. `S_ANOMALY_RUN_A006=1 python -m unittest discover -s app/tests/acceptance -t app -p "test_a006_performance.py" -v`
     * 生成は `app/tests/acceptance/test_a006_performance.py` の `SCALE`(365 日 / DB 10×6 / JVM 24 / LSF 12)で行われる
  3. ★訂正 2026-09-03 / P203★ 当初ここに「S4 取り込みが約 2 秒」と書いたが、**最後の S4 ログ行から S5 の行までを測った誤り**であった(S4 の開始点を取り違えていた)。測り直した正しい内訳は `docs/P202-fix-plan.md` 5.4 を参照。**91 日規模では S6(検知)が支配的だが、365 日規模では S4(取り込み)が最大になる。**

- 影響がありそうなモジュール:
  * **`app/src/s_anomaly/detectors/` の S6 検知ループが支配的**である。52 万レコードでの内訳(S6 が全体の 67%)と、中断時に 1 コア専有で回り続けていた事実の両方がこれを指す
  * A006【失敗時に記録する内容】が最初に疑うよう指示している 2 つは、次のとおり:
    * **`ALG-B2` の O(n²)**: `mann_kendall` は全ペア走査であり、バケット数 n に対して O(n²)。`max_buckets = 2000` のとき 1 系列あたり約 200 万回の比較になる。96 系列 × 11 メトリクスで効く
    * **`ALG-A2` の窓ごとの全走査**: Hampel は窓ごとに中央値と MAD を求めるため、点数 × 窓幅に比例する
  * S4(取り込み)は ADR-011 の一時 CSV + `read_csv` 方式により十分速く、**支配要因ではない**
- テスト指示との矛盾の疑い: **あり(NFR-002 の目標値そのもの)**。`docs/P001-requirement.md` NFR-002「1,000 万レコードで 10 分以内」は ★FIXME★ が付された **Agent の想定値**であり、実運用の規模・許容時間の裏付けがない。P202 では「実装の問題か、目標値の問題か」の切り分けが必要である(`docs/P009-acceptance-direction.md` 6章 #2)。**本記録の時点では切り分けを行わず、実測値のみを提示する**
- 次工程への申し送り:
  * **P202(修正計画)へ引き渡す。** 切り分けの材料として、規模別の実測内訳と、疑わしいアルゴリズムを添える
  * **測定手順の欠陥を 1 つ申し送る。** 本テストは `time.monotonic()` で壁時計を測っているが、**実行中にマシンがスリープすると経過時間に休止時間が算入され、測定値が使えなくなる**(今回、経過 1,079.9 分に対し CPU は 101.3 分)。再測定時は **CPU 時間を測る**か、スリープを抑止した状態で実行する必要がある。あわせて `subprocess.run(timeout=7200)` も発火しなかった
  * **本テストは既定でスキップする設定に変更した。** 1 回の実行に数時間を要し、受け入れスイート全体をブロックするためである。A006【実行コマンド】が「実行に長時間かかるため、`unittest` の他のテストと分けて実行してよい」と認めている範囲の措置であり、**失敗を隠す目的ではない。結果は本記録のとおり FAIL である。** 実行するには `S_ANOMALY_RUN_A006=1` を設定する

> **【訂正 2026-09-03 / P203】規模の記載を訂正した。** 当初「10,091,520 レコード」と記載していたが、これは誤りであった。テストの `rescale()` が規模を **系列数 x 点数**で数えており、(a) `gen_testdata.generate_normal` が `write_jstat` を **app01 / app02 の 2 つに固定**して呼び`JVM_PAIRS` を反復しないため `jvm_pairs=24` が生成に効いていなかったこと、(b) `bqueues` はファイル上では 12 キューを 1 行に横持ちすること、の 2 点を見落としていた。**実際の規模はテーブル行 7,778,880 行 / ファイル上のデータ行 6,622,560 行である**(ファイル数 1,460・サイズ 336MB は記載どおり)。**目標の 1,000 万レコードには到達していなかった。それより小さい規模で 2 時間を超えている**ため、FAIL の判定は変わらない(むしろ超過の度合いは記載より大きい)。`app/tests/acceptance/test_a006_performance.py` の `SCALE` と `rescale()` はP203 で訂正済みであり、現在は目標規模(テーブル行 11,563,200)に到達する。

## A007: メモリ制限下での退避

- 実行日時: 2026-09-03 00:00
- 実行者: Reviewer (P201)
- 参照したテスト指示: docs/P009-acceptance-direction/A007-memory-spill.md
- 実行環境: Windows 11 Pro / Python 3.14.2 / DuckDB 1.5.5
- 事前ビルド結果: 成功
- 実行コマンド: `python -m unittest discover -s app/tests/acceptance -t app -p "test_a007_memory_spill.py" -v`
- 結果: **PASS** (9 tests / 137.2s)
- 期待結果: A007【期待結果】の 9 項目
- 実際の結果: 9 項目すべてが期待値と一致した。

  | ケース | `max_memory` | 終了コード | 備考 |
  | --- | --- | --- | --- |
  | 1 | 4GB | 0 | 基準 |
  | 2 | 256MB | **0** | **退避して完走(NFR-003)**。`tmp/` が作られていた |
  | 3 | 1KB | **6** | `StorageError`。**99 ではない** |

  * **ケース 1 とケース 2 の `report.md` が実行日時の行を除いて完全一致**した。**メモリ制限が結果を変えていない**
  * ケース 2 の標準エラーに `MemoryError` / `OutOfMemoryException` / `Traceback` は出ていない
  * ケース 3 の標準エラーに `max_memory` の現在値(`1KB`)・`temp_directory`・対処案内が出ている(DS-01-04)
  * 存在しない `temp_directory`(`./deep/nested/spill`)を指定しても自動的に作られ、終了コード 0 で完走した(DS-14-04)
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- テスト指示との矛盾の疑い: **あり(A002・A004 と同根)**。A007【事前準備】3 は「各作業ディレクトリに `settings.properties` を置く」としているが、UI-02-L01 により設定は `s_anomaly.py` と同じディレクトリから読まれる。ケースごとに `app/` を複製し、複製側の設定を差し替えて実施した
- 次工程への申し送り: なし

## A008: ネットワーク非通信の確認

- 実行日時: 2026-09-03 00:00
- 実行者: Reviewer (P201)
- 参照したテスト指示: docs/P009-acceptance-direction/A008-no-network.md
- 実行環境: Windows 11 Pro / Python 3.14.2 / **DuckDB 1.5.5**
- 事前ビルド結果: 成功
- 実行コマンド: `python -m unittest discover -s app/tests/acceptance -t app -p "test_a008_no_network.py" -v`
- 結果: **PASS** (9 tests / 48.4s)
- 期待結果: A008【期待結果】の 8 項目
- 実際の結果: 8 項目すべてが期待値と一致した。
  * **静的確認は `ast.parse` で行い、文字列 grep は使っていない**(A008【実行手順】3 の指示どおり)
  * 走査対象は `app/src/` と `app/tools/` と `app/s_anomaly.py`(`app/tests/` は対象外)
  * **禁止モジュールの import 0 件**: `socket` / `ssl` / `urllib` / `http` / `requests` / `ftplib` / `smtplib` / `telnetlib` / `xmlrpc` / `asyncio` / `select` / `selectors` / `webbrowser`
  * **外部プロセス起動の import 0 件**: `subprocess` / `multiprocessing` / `pty`。`os.system` / `os.popen` / `os.execv` / `os.spawnv` の呼び出しも 0 件
  * **`duckdb` 以外のサードパーティ import 0 件**(FR-003)
  * `autoinstall_known_extensions` = **false**、`autoload_known_extensions` = **false**(DuckDB 1.5.5 で両設定とも存在し、`false` であることを確認した)
  * 実行時の標準エラーに接続失敗・タイムアウト・プロキシ関連の文言なし。実行時間は A001 と同程度。終了コード 0
  * 走査が実際にソース全体を覆っていること(ファイル数 25 以上、`bootstrap.py` と `gen_testdata.py` を含む)も確認した
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- テスト指示との矛盾の疑い: なし
- 次工程への申し送り: 本テストは A008 の ★ACCEPTED★ のとおり**静的確認 + 設定確認 + 実行時確認**で行っており、**実際に通信を遮断した環境での実行は行っていない**。DuckDB 内部が拡張以外の経路で通信する場合は見逃す。**閉域環境での初回実行時に人間が確認する項目として P302 へ引き継ぐ**

## A009: 破損データでの完走

- 実行日時: 2026-09-03 00:04
- 実行者: Reviewer (P201)
- 参照したテスト指示: docs/P009-acceptance-direction/A009-broken-input.md
- 実行環境: Windows 11 Pro / Python 3.14.2 / DuckDB 1.5.5
- 事前ビルド結果: 成功
- 実行コマンド: `python -m unittest discover -s app/tests/acceptance -t app -p "test_a009_broken_input.py" -v`
- 結果: **PASS** (12 tests / 2.5s)
- 期待結果: A009【期待結果】の 11 項目
- 実際の結果: 11 項目すべてが期待値と一致した。
  * ケース1(破損のみ)・ケース2(正常 3 ファイル + 破損全部の混在)とも**終了コード 0**(FR-091)
  * 標準エラーの WARNING に**ファイル名・理由・件数**が出ている
  * `Traceback` なし
  * **4.1 節の内容が `gen_testdata.BROKEN_EXPECTATIONS` と完全一致**:

    | ファイル | 理由と件数 |
    | --- | --- |
    | DBConnection_20260601.csv | 列数不一致 2 / 数値変換不能 2 / 日付書式不正 2 |
    | bqueues_host1_20260601.txt | 見出し不明 1 |
    | bqueues_host2_20260601.txt | 日付書式不正 2 |
    | broken01_gc_host1_20260601.txt | 見出し不明 1 |
    | broken02_gc_host1_20260601.txt | 列数不一致 2 |

  * 理由コードは 4 種に収まっている
  * 混在ケースで総レコード数が増えており、**破損ファイルの存在で正常データが失われていない**。4.1 節に正常ファイルは載っていない
  * **BOM 付き UTF-8 / CRLF のファイルはエラーとして記録されていない**(NFR-007)。空ファイル・見出しのみのファイルも同様
- 失敗ログ抜粋: 初回実行で 3 件が失敗したが、**いずれも本フェーズで新規作成したテストコード側の欠陥であり、アプリケーションの欠陥ではなかった**。原因を特定して修正し、再実行して全件 PASS を確認した。**アプリケーションコードは変更していない。期待値(`BROKEN_EXPECTATIONS`)も変更していない。**

  ```
  FAIL: test_03_case1_warnings_have_file_reason_count
    AssertionError: Regex didn't match: '\d+ 件'
    -> アプリの出力は「列数不一致 2件」であり、数字と「件」の間に空白は入らない。
       テスト側の正規表現が誤り。ファイル名・理由・件数はいずれも出力されている。
       正規表現を r"\d+\s*件" に修正した。

  FAIL: test_08_case2_keeps_normal_records
    AssertionError: 25 not greater than 25
  FAIL: test_09_case2_lists_only_broken_files
    AssertionError: 'DBConnection_20260601.csv' unexpectedly found in {...}
    -> 混在ディレクトリの組み立てが誤り。正常系と破損系はどちらも
       DBConnection_20260601〜05.csv を持つため、正常ファイルを名前順の先頭から
       3 つ選ぶと、後からコピーした破損側が正常側を上書きし「混在」にならなかった。
       破損側と名前が衝突しない正常ファイルを選ぶよう修正し、
       「混在ディレクトリが実際に両方を含むこと」を確認するテストを追加した。
  ```

- 再現手順: 上記のとおり(修正済み)
- 影響がありそうなモジュール: なし(アプリ側に問題は検出されなかった)
- テスト指示との矛盾の疑い: なし
- 次工程への申し送り: なし

## A010: 配布物としての可搬性

- 実行日時: 2026-09-03 00:06
- 実行者: Reviewer (P201)
- 参照したテスト指示: docs/P009-acceptance-direction/A010-portability.md
- 実行環境: Windows 11 Pro / Python 3.14.2 / DuckDB 1.5.5
- 事前ビルド結果: 成功
- 実行コマンド: `python -m unittest discover -s app/tests/acceptance -t app -p "test_a010_portability.py" -v`
- 結果: **PASS** (10 tests / 157.8s)
- 期待結果: A010【期待結果】の 10 項目
- 実際の結果: 10 項目すべてが期待値と一致した。

  | ケース | 内容 | 結果 |
  | --- | --- | --- |
  | A | リポジトリ外へコピーして実行 | 終了コード 0。`report.md` 生成。A001 の結果と、実行日時・対象ディレクトリの行を除いて**完全一致** |
  | B | `vendor/` 不一致 | **終了コード 7**。`report.md` は生成されない |
  | C | `tests/` を丸ごと削除 | **終了コード 0**。結果はケース A と同一 |

  * **項目 4(フォールバックの記録)を確認した。** 起動ログと `report.md` 1 章の「実行環境の注記」の**両方**に「vendor を使わず環境の DuckDB を使用しています」が出ている(DS-14-02 の ★ACCEPTED★ が要求)
  * `pip install` を一切行わずに動作している。コピー先に `setup.py` / `requirements.txt` は無い
  * ケース B の標準エラーに **4 要素すべて**が含まれる: 期待した vendor ディレクトリの絶対パス / 実行環境のタグ(`win-amd64-cp314`) / 用意されている vendor の一覧(手順 5 で作った `linux-x86_64-cp39`, `win-amd64-cp38` を含む) / README への案内
  * ケース C が成功したことで、**`src/` が `tests/` に依存していない**ことが確認できた
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- テスト指示との矛盾の疑い: **あり(A002 項目 11 と同根)**。A010【テスト指示との矛盾の疑い】欄が予告したとおり、**ケース B の再現方法(`vendor/{tag}/duckdb/__init__.py` に `raise ImportError`)だけでは終了コード 7 にならない**。実装は vendor の失敗後に環境の DuckDB へフォールバックする設計(DS-14-02)であり、これは正しい挙動である。`PYTHONPATH` で環境側の `duckdb` も隠して再現した
- 次工程への申し送り: **`vendor/` からの実際の読み込みは本テストでは確認できていない。** `vendor/` の実体(DuckDB の wheel)は配布先の Python 版が未確定であるため用意していない(`docs/P005-impl-plan.md` 7章の ★ACCEPTED★)。確認できたのは「フォールバック経路での動作」と「不一致時の終了コード 7 とメッセージ」の 2 つのみである。**閉域環境で `vendor/` から読む経路が未検証のまま納品される残存リスクがあり、P302 の実行前チェックで必ず確認すること。**

## A011: 一般ユーザー権限での実行

- 実行日時: 2026-09-03 00:09
- 実行者: Reviewer (P201)
- 参照したテスト指示: docs/P009-acceptance-direction/A011-user-privilege.md
- 実行環境: Windows 11 Pro / Python 3.14.2 / DuckDB 1.5.5
- **実行者の権限: 管理者ではない**(`ctypes.windll.shell32.IsUserAnAdmin()` が 0)
- 事前ビルド結果: 成功
- 実行コマンド: `python -m unittest discover -s app/tests/acceptance -t app -p "test_a011_user_privilege.py" -v`
- 結果: **PASS** (9 tests / 52.8s)
- 期待結果: A011【期待結果】の 8 項目
- 実際の結果: 8 項目すべてが期待値と一致した。
  * 管理者/root ではない状態で終了コード 0
  * 標準エラーに `PermissionError` / `Access is denied` / `アクセスが拒否されました` / `Operation not permitted` なし
  * **入力ディレクトリが実行前後で不変**(ファイル一覧・サイズ・更新日時)
  * **`app/` 配下(`__pycache__` と `.pyc` を除く)が実行前後で不変。`app/tests/_work/` も変更されていない**
  * 作業ディレクトリに増えたのは `report.md` と `tmp/` 配下のみ
  * ユーザーのホーム直下・リポジトリ直下に変化なし
  * サブプロセスがブロックせずに終了しており、UAC の昇格プロンプトは出ていない
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- テスト指示との矛盾の疑い: **あり(★FIXME★ が予告したとおり)**。A011【事前準備】4 の ★FIXME★ が指摘するとおり、**Windows のディレクトリに対する読み取り専用属性は配下のファイル作成を必ずしも防がない**。ACL による拒否は実行環境によっては設定できないため、**指示が併記している代替手段「実行前後のファイル一覧の比較」で確認した**(入力ファイル自体には読み取り専用属性を付与している)
- 次工程への申し送り: なし

---

## P201 の総括

* **A001〜A011 のうち 10 件が PASS、1 件(A006)が FAIL。BLOCKED / NOT RUN は 0 件。**
* **A006 の FAIL により、P202(修正計画)への引き渡しが必要である。**
* 実行中に**アプリケーションコードを一切修正していない。** 行ったのは (a) 本フェーズの指示に従ったテストコードの新規作成、(b) A009 で判明した**テストコード側の欠陥の是正**(記録は A009 の欄)の 2 つのみである。
* `app/tests/_work/normal/expected.json` および各テストの期待値表は**書き換えていない**。
* **テスト指示側の問題を 6 件記録した**(A001・A002 に 2 件・A004・A007・A010・A011)。うち **UI-02-L01(設定ファイルの置き場所)に関するもの 3 件と、vendor フォールバックに関するもの 2 件は同根**であり、P202 でまとめて扱えるものである。
* **スイートの再実行可能性を確認した(TP-19)。** 受け入れスイート全体を続けて 2 回実行し、
  いずれも同じ結果になった。永続化データストアを共有していないため、
  前回実行分のデータが残って 2 回目だけ落ちる事象は起きていない。

  ```
  python -m unittest discover -s app/tests/acceptance -t app

  1 回目 -> Ran 118 tests in 630.567s / OK (skipped=9)
  2 回目 -> Ran 118 tests in 556.944s / OK (skipped=9)
  ```

  `skipped=9` は A006 である(既定でスキップ。結果は本記録のとおり FAIL)。
