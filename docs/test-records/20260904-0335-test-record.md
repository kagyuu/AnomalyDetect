# テスト記録 — 2026-09-04 03:35(P205 結合テスト再実施 / Reviewer Loop 1 回目)

* 実施フェーズ: **P205(結合テスト再実施)**
* 入力: `docs/P008-test-direction.md`, `docs/P009-acceptance-direction.md`, `docs/P202-fix-plan/fixed/F001-alg-b4-window-scan.md`, `docs/P202-fix-plan/fixed/F002-test-direction-corrections.md`, `docs/P204-impact-analysis.md`
* 直前の記録: `docs/test-records/20260903-0010-test-record.md`(P201 1 回目)

## 再実行の対象

`SKILL-P205-integration-retest.md` の定めに従い、**直近のテスト記録で FAIL / BLOCKED だったテスト、および P204 で影響が疑われたテスト**に限って再実行した。全件の無条件再実行は行っていない。

| 区分 | 対象 | 理由 |
| --- | --- | --- |
| 直近 FAIL | **A006** | `20260903-0010-test-record.md` で唯一の FAIL |
| P204 で影響が疑われたテスト | **なし** | `docs/P204-impact-analysis.md` 5 章が「壊している疑いなし」と判定 |

**ただし P203 の検証時に、単体 376 件・結合 105 件・受け入れ 109 件をいずれも連続 2 回実行して全件成功を確認済みであり、その後ソースコードは 1 行も変更していない。** 本記録ではその結果も参考として併記する。

---

## A006: 大規模データでの完走と実行時間

- 実行日時: 2026-09-04 02:22〜03:35
- 実行者: Reviewer Loop (P205)
- 対象ブランチ/コミット: (git 未初期化のため該当なし。`app/` の差分は退避物 `s_anomaly-20260903004204.zip` との比較で `b4_cusum.py` と `test_a006_performance.py` の 2 ファイルのみ)
- 参照したテスト指示: `docs/P009-acceptance-direction/A006-performance.md`
- 実行環境: Windows 11 Pro 10.0.26200 / Python 3.14 / DuckDB / 物理メモリ 51.2GB / 空き容量 592GB
- 事前ビルド結果: `python -m compileall -q app` 終了コード 0
- 実行コマンド:

  ```
  S_ANOMALY_RUN_A006=1 python -m unittest discover -s app/tests/acceptance -t app -p "test_a006_performance.py" -v
  ```

- **結果: FAIL**
- 期待結果:
  * 終了コード 0 で完走し `report.md` を生成すること
  * 大規模ケース(90 日分 / テーブル 2,851,200 行 / 360 ファイル)で **600 秒以内**(NFR-002)
  * 既定のメモリ上限を超えた分は `temp_directory` へ退避して処理を継続すること(NFR-003 / P000 2章)
- 実際の結果: `Ran 13 tests` / **`FAILED (failures=7)`** を **連続 2 回**(いずれも同一の失敗内容)

  | 実行 | 通常 30 日 | 大規模 90 日 | 壁時計 | CPU 秒ピーク |
  | --- | --- | --- | --- | --- |
  | 1 回目 | 終了コード 6 / 373.9s | 終了コード 6 / 828.7s | 1,235.5s | 1,656.5s |
  | 2 回目 | 終了コード 6 / 387.7s | 終了コード 6 / 847.2s | (同等) | (同等) |

  失敗した 7 件: `TestA006NormalUseCase` の `test_01_completes_with_exit_zero` / `test_02_report_structure` / `test_03_record_count_matches`、`TestA006Performance` の `test_01_completes_with_exit_zero` / `test_02_record_count_matches` / `test_03_within_target_time` / `test_04_report_structure`。

  合格した 6 件: `test_00_scale_is_the_large_use_case`、`test_04_scale_is_the_normal_use_case`、`test_05_progress_is_continuous`、`test_06_no_memory_error`、`test_07_temp_files_do_not_block_rerun`、`test_08_file_count_within_nfr001`。

  **壁時計(1,235.5s)と CPU 秒(1,656.5s)が整合しており、マシンのスリープによる測定値の破綻は起きていない**(P201 の 1,079.9 分 / CPU 101.3 分のような乖離がない)。A006 指示書【検証コマンド】の注意点を満たしている。

- 失敗ログ抜粋:

  ```
  2026-09-04 03:07:57 [INFO] [S7] 検知点 43872 点を 7229 件のイベントに統合しました
                                  (SEVERE: 416, FATAL: 2898, WARN: 3915, INFO: 0)
  2026-09-04 03:12:47 [ERROR] [--] error: データストアの処理に失敗しました
    理由: Out of Memory Error: failed to pin block of size 256.0 KiB (3.7 GiB/3.7 GiB used)

  Possible solutions:
  * Reducing the number of threads (SET threads=X)
  * Disabling insertion-order preservation (SET preserve_insertion_order=false)
  * Increasing the memory limit (SET memory_limit=...GB)

    現在の設定: [duckdb] max_memory = 4GB, temp_directory = ./tmp
  ```

  ```
  AssertionError: 828.6853863000288 not less than or equal to 600.0 :
    NFR-002 未達: 90 日分 / 2,851,200 行で 828.7 秒 (目標 600 秒)
  ```

  **注意**: この 828.7 秒は**異常終了までの時間**であり、完走に要する時間ではない。完走時の実測は下記の切り分けにある 876.2 秒である。

- 再現手順:

  1. `gen_testdata` のモジュール定数を 90 日分(`db_hosts=16` / `db_sources=6` / `lsf_queues=12`)に差し替えて生成する(`test_a006_performance.py::rescale`)。
  2. `app/` を一時ディレクトリへ複製し、複製側の `settings.properties` に `[duckdb] max_memory = 4GB` / `temp_directory = ./tmp` を置く。
  3. 複製した `s_anomaly.py` に生成先の `logs` を渡して実行する。

  **4 回とも同じ箇所・同じメッセージで再現した**(通常 30 日 × 2、大規模 90 日 × 2)。

- 影響がありそうなモジュール:
  * **`app/src/s_anomaly/correlate.py` の `_aggregate_baseline`(最有力)** — `_event_windows` × `range(1,8)` の CROSS JOIN × `metrics` の範囲結合。計算量は O(イベント数 × 基準日数 × 系列数 × 窓内の点数)。通常 30 日分の概算で約 1.97 億行を突き合わせる
  * `app/src/s_anomaly/correlate.py` の `_aggregate_current`(同じ範囲結合を基準日数分なしで行う)
  * `app/src/s_anomaly/bootstrap.py`(`max_memory` / `temp_directory` / `preserve_insertion_order` の設定)
  * `app/src/s_anomaly/config.py`(`threads` が既知キーに無い)
  * `app/src/s_anomaly/detectors/b4_cusum.py`(検知点の 76% を出しており、S8 の入力量を決めている)

- **テスト指示との矛盾の疑い: あり**

  `docs/P001-requirement.md` **NFR-003**「メモリ 既定で最大 4GB。**超過分は `temp_directory` へ退避して処理を継続する**(P000 2章の指定に対応)」に対し、**実装は退避に失敗して異常終了している**。

  出典の `docs/P000-concept-analysis.md` 2章は人間の記述であり「DuckDB インメモリ(**+メモリからあふれたら temp ファイルを使う**)」とある。**「あふれたら退避して継続する」という挙動は人間が指定したものである**(ただし 4GB という具体値は Agent の想定であり、`docs/P001-requirement.md` 15章冒頭の包括的な ★FIXME★ が掛かる)。

  **これはテスト指示側の誤りではなく、要件に対するアプリケーションコードの欠陥である。** 上限値をいくつにしても、その上限で「あふれたら退避して継続」が成立しなければ NFR-003 を満たさない。

  あわせて `docs/P001-requirement.md` **NFR-002**「1,000 万レコードで 10 分以内」は ★FIXME★ 付きの想定値であり、**確定した期間(大規模 90 日)ではテーブル 2,851,200 行にしかならず、前提そのものが実態と合っていない。**

- 次工程への申し送り:

  **P202 へ差し戻し、S8 の修正タスクを起票すること。** 詳細・対処案 5 件・人間への確認事項は `docs/P202-fix-plan/P202-fix-unresolved.md` に記録済みである。推奨は案 1(S8 の集計をイベントのバッチに分割して中間結果を有界にする)。

### 参考: 原因切り分けのための追加測定(P203 実施)

`settings.properties` の `max_memory` のみを変えて測定した(**アプリケーションコードは変更していない**)。いずれも一時ディレクトリへ複製した配布物に対して実行した。

| ケース | テーブル行 | ファイル | max_memory | 終了コード | 所要 | イベント | `report.md` |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 通常 30 日 | 950,400 | 120 | **4GB(既定)** | **6** | 363.6s で異常終了 | — | **生成されず** |
| 通常 30 日 | 950,400 | 120 | 16GB | 0 | **191.3s** | 7,229 | 14,393,784 バイト |
| 通常 30 日 | 950,400 | 120 | 32GB | 0 | 192.0s | 7,229 | 14,393,784 バイト |
| 大規模 90 日 | 2,851,200 | 360 | 16GB | 0 | **876.2s** | 21,307 | 41,213,665 バイト |

ステップ別の内訳:

| ステップ | 通常 30 日 / 4GB | 通常 30 日 / 16GB | 大規模 90 日 / 16GB |
| --- | --- | --- | --- |
| S4 取り込み | 27s | 33s | 80s |
| S6 検知 | 45s | 41s | 152s |
| S7 統合 | 約 1s | 約 1s | 約 1s |
| **S8 相関** | **290s 経過後に OOM** | **114s(60%)** | **635s(72%)** |
| 合計 | 363.6s(失敗) | 191.3s | 876.2s |

* **16GB と 32GB が同値であることから、16GB で足りている。**
* **イベント数 7,229 → 21,307(2.95 倍)に対し S8 は 114s → 635s(5.6 倍)と超線形に伸びる。**
* **大規模 90 日では S8 だけで 635 秒であり、S8 単独で NFR-002 の 600 秒を超える。**
* `threads` は `config.py` の既知キーに無く、`settings.properties` に書いても「未知のキーを無視しました」となる。DuckDB が提示する 3 つの対処のうち `preserve_insertion_order = false` は適用済み(`bootstrap.py` 155 行)。

### 参考: S6 のアルゴリズム別内訳(F001 の効果)

| アルゴリズム | 通常 30 日 検知点 / 所要 | 大規模 90 日 検知点 / 所要 |
| --- | --- | --- |
| ALG-A1 | 6,673 / 3.7s | 19,599 / 16.1s |
| ALG-A2 | 404 / 6.2s | 1,215 / 19.0s |
| ALG-A3 | 5 / 2.6s | 15 / 11.4s |
| ALG-A4 | 3,216 / 4.5s | 10,077 / 18.3s |
| ALG-A5 | 35 / 4.6s | 245 / 18.1s |
| ALG-B1 | 12 / 4.1s | **0** / 14.9s |
| ALG-B2 | 8 / 5.0s | 8 / 16.2s |
| ALG-B3 | 59 / 2.6s | 165 / 11.8s |
| **ALG-B4** | **33,456 / 6.3s** | **99,316 / 16.8s** |
| ALG-B5 | 4 / 4.1s | 4 / 8.4s |
| 合計 | 43,872 / 45s | 130,644 / 152s |

**F001 は効いている。** 修正前 ALG-B4 は S6 の 75%・全体の 53% を占めていた(524K レコードで 132.8s)。修正後は S6 45 秒のうち 6.3 秒(14%)であり、支配要因ではない。

**あわせて 2 件の観察が確定規模で再現した**(`docs/P202-fix-plan/P202-fix-unresolved.md` の付記)。

* ALG-B4 の検知点が全体の 76% を占め、`report.md` が 30 日分で 14.4MB / 90 日分で 41.2MB になる
* ALG-B1 が 90 日分で検知 0 件になる(14 日 6 点 → 30 日 12 点 → 90 日 0 点)

---

## 参考: A006 以外のテスト(P203 の検証時に実行。本フェーズでは再実行していない)

`SKILL-P205-integration-retest.md` は「全件を無条件に再実行する必要はない」と定める。P204 が影響を疑ったテストは無く、**P203 の検証実行後にソースコードは変更していない**ため、以下をそのまま判定材料とする。CLAUDE.md の「続けて 2 回実行して同じ結果になることを確認する」規則に従い、いずれも連続 2 回実行した。

| 対象 | 1 回目 | 2 回目 | 結果 |
| --- | --- | --- | --- |
| ビルド `python -m compileall -q app` | 終了コード 0 | — | PASS |
| 単体テスト | `Ran 376 tests in 629.860s` / `OK (skipped=1)` | `Ran 376 tests in 608.501s` / `OK (skipped=1)` | PASS |
| 結合テスト(T001〜) | `Ran 105 tests in 1057.719s` / `OK` | `Ran 105 tests in 676.081s` / `OK` | PASS |
| 受け入れ結合テスト(A001〜A011。A006 を除く) | `Ran 122 tests in 501.553s` / `OK (skipped=13)` | `Ran 122 tests in 510.945s` / `OK (skipped=13)` | PASS |

* 件数(376 / 105 / 122)が出ており、パス指定の誤りで 0 件が静かにスキップされてはいない。
* `skipped=13` は A006 の 2 クラス(大規模 9 件 + 通常 4 件)である。**実行されたのは 109 件。**
* 2 回目が 1 回目と同じ結果であり、永続化データの残留による失敗は起きていない。

### F001 の検知結果不変性の確認(P203 実施)

退避物の修正前ツリーと現行ツリーを別々の一時ディレクトリへ展開し、同一入力(`app/tests/_work/normal/logs`)で実行した結果、`report.md` は**双方 477,751 バイト**で、**実行日時の行を除く 9,107 行が完全一致**した。**検知結果は修正前と変わっていない。**

---

## P201 の再判定(P205 の責務)

`SKILL-P205-integration-retest.md` は「再実行結果を記録したら、P201(実装横断レビュー)を再実行し、全件 PASS かどうかを再判定する」と定める。

| 区分 | 件数 | 結果 |
| --- | --- | --- |
| 単体テスト | 376 | 全件 PASS |
| 結合テスト | 105 | 全件 PASS |
| 受け入れ結合テスト(A001〜A005, A007〜A011) | 109 | 全件 PASS |
| **受け入れ結合テスト A006** | 13 | **FAIL(7 件)** |

**判定: 全件 PASS ではない。したがって P202(修正計画)へ差し戻す。**

* Reviewer Loop の停止条件は「**P201 を 3 回実行しても**全件 PASS にならない場合」である。
* **P201 の実行はこれで 2 回目**(1 回目 = `20260903-0010-test-record.md`、2 回目 = 本記録の再判定)。**停止条件には該当しない。処理は止めず P202 へ進む。**
* P202 が起票すべき修正タスクは 1 件: **S8(相関の集計)がメモリ上限内で完走しない欠陥。** 対処案は `docs/P202-fix-plan/P202-fix-unresolved.md` に 5 案を実測付きで整理済み。
