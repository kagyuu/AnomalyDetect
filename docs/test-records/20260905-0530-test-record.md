# テスト記録 — 2026-09-05(P903 内部の P103 / P201 / P205。CR-001〜CR-004 対応)

* 実施フェーズ: **P903 内部の P103(結合テスト実行)および P201 / P205(実装横断レビュー)**
* 対象 CR: CR-001 / CR-002 / CR-003 / CR-004
* 入力: `docs/P008-test-direction.md`, `docs/P009-acceptance-direction.md`, `docs/P903-cr-records/CR-00N.md`
* 直前の記録: `docs/test-records/20260904-0530-test-record.md`

---

## 1. 実行したテストと結果

CLAUDE.md の「テストスイートは続けて 2 回実行して同じ結果になることを確認する」規則に従い、
**全スイートを連続 2 回**実行した。

| 対象 | 1 回目 | 2 回目 | 結果 |
| --- | --- | --- | --- |
| ビルド `python -m compileall -q app` | 終了コード 0 | — | PASS |
| 単体テスト | `Ran 410 tests in 201.010s` / `OK (skipped=1)` | `Ran 410 tests in 213.203s` / `OK (skipped=1)` | **PASS** |
| 結合テスト | `Ran 108 tests in 261.665s` / `OK` | `Ran 108 tests in 274.864s` / `OK` | **PASS** |
| 受け入れ結合テスト | `Ran 125 tests in 271.343s` / `OK (skipped=15)` | `Ran 125 tests in 273.744s` / `OK (skipped=15)` | **PASS** |

* 件数(410 / 108 / 125)が出ており、パス指定の誤りで 0 件が静かにスキップされてはいない。
* 2 回目が 1 回目と同じ結果であり、永続化データの残留による失敗は起きていない。
* `skipped=15` は A006 の 2 クラス(大規模 11 件 + 通常 4 件)である。**実行されたのは 110 件。**

### 件数が増えた理由

| 区分 | CR 前 | CR 後 | 増加分 |
| --- | --- | --- | --- |
| 単体 | 376 | **410** | `test_co_anomaly.py` を新設(22 件)、ID 採番・レポート分割・集計の確認を追加 |
| 結合 | 105 | **108** | サマリの章構成、`co_anomaly` が `metrics` を触らないこと、などを追加 |
| 受け入れ | 122 | **125** | A006 に 4K の確認とホスト分割の確認を追加 |

**期待値を弱めた箇所はない。** 変更したのは、CR により**外部仕様が変わった箇所の期待値**である
(章番号 4 → 5、`report.md` → `report_*.md`、イベント ID の形式、項目名)。
各変更には根拠となる仕様箇所を `※CR-00N` の注記で明記した。

---

## 2. A006: 大規模データでの完走と実行時間

- 実行日時: 2026-09-05 05:0x〜06:0x
- 実行者: P903 内部の P201 / P205
- 参照したテスト指示: `docs/P009-acceptance-direction/A006-performance.md`
- 実行環境: Windows 11 Pro 10.0.26200 / Python 3.14 / DuckDB 1.5.5 / 物理メモリ 51.2GB
- 実行コマンド: `S_ANOMALY_RUN_A006=1 python -m unittest discover -s app/tests/acceptance -t app -p "test_a006_performance.py" -v`
- **結果: PASS**

### 2.1 性能(既定の `max_memory = 4GB`)

| ケース | テーブル行 | ファイル | CR 適用前 | **CR 適用後** | 改善 |
| --- | --- | --- | --- | --- | --- |
| 通常 30 日 | 950,400 | 120 | 147.4s | **70.8s** | **-52%** |
| 大規模 90 日 | 2,851,200 | 360 | 531.3s | **220.4s** | **-59%** |

**NFR-002(600 秒)に対する余裕が 11% から 63% へ広がった。**

改善の主因は **CR-002 による S8 の置き換え**である。従来は `metrics` テーブル(90 日規模で
約 400 万行)に対して「イベントの窓 × 基準 7 日 × 全系列」の範囲結合を行っていたが、
**検知済みイベント同士の突き合わせ(掃引法)**になり、`metrics` を一切参照しなくなった。

### 2.2 出力(CR-001 / CR-004)

| 規模 | ファイル数 | 内訳 |
| --- | --- | --- |
| 通常 30 日 | 18 | ホスト別 17 + サマリ 1 |
| 大規模 90 日 | 54 | ホスト別 51(17 ホスト × 3 か月)+ サマリ 3 |

### 2.3 4K トークンの達成状況(90 日規模の実測)

| 出力 | サイズ | 目安 4,000 バイト |
| --- | --- | --- |
| `report_summary_202606.md` | **3,830 バイト** | ○ |
| `report_summary_202607.md` | 3,813 バイト | ○ |
| `report_summary_202608.md` | 3,793 バイト | ○ |
| ホスト別ファイルの「1. このホストの集計」(最大) | **674 バイト** | ○ |

★FIXME★ **トークン数はバイト数で代用している。** トークナイザが配布資産に含まれないため
(FR-003 により外部依存は DuckDB のみ)、厳密な計測ができない。
**実際のローカル LLM のトークナイザでの確認が必要である。**

### 2.4 4K に収めるために要した是正

**当初の実装では 90 日規模で 7,254 バイトになり、目標を達成できなかった。**

| 章 | 当初 | 是正後 | 主因 |
| --- | --- | --- | --- |
| 2. ホスト別の集計 | 1,593 バイト(全 17 ホスト) | 上限 10 行 | ホスト数に比例して増える |
| 3. 日別のイベント数 | 1,045 バイト(全 30 日) | 上限 10 行(件数順) | 日数に比例して増える |
| **4. 複数ホストで同時に発生したアノマリー** | **3,683 バイト** | 上限 8 行 + 1 行内 3 件 | **16 ホスト分の名前を 1 行に列挙していた** |

**すべての表に行数上限を置き、規模が伸びても増えない構造にした**(`docs/P003-backend-spec.md` DS-12-08)。

---

## 3. 実装中に発見し、その場で是正した設計上の欠陥

**いずれもテストの実行によって発見した。** 詳細は `docs/P903-cr-records/CR-002.md` の
「事前のスコープ想定と実際の差異」にある。

| # | 発見した欠陥 | 是正 |
| --- | --- | --- |
| 1 | **原因候補ルール CR-01 / CR-02 / CR-04 / CR-06 が構造的に発火しなくなった。** 観点2 を主体とするルールなのに、観点2 のイベントには同時アノマリーを設定しない設計にしたため | `co_anomaly` の出力を 2 系統に分けた。`co_anomalies`(レポート用・観点1 のみ)と `concurrent_by_metric`(原因候補用・観点を問わない・**レポートには出さない**) |
| 2 | 是正 1 のあとも **CR-01 が発火しなかった**。14 日間のイベントには期間中のあらゆるイベントが重なるため、`eu` の `level_shift` が「変化あり」と判定されていた | 否定条件の判定を**持続的な上昇(`trend_up` / `floor_rise`)が無いこと**に限定した |
| 3 | **破損データの読み飛ばし件数が失われた**(FR-014 違反)。検知 0 件だとホスト別ファイルが作られず、注意事項の記録先が消えていた | サマリに「5. 実行時の注意事項」を追加した(UI-04-F10 の実装漏れ) |
| 4 | **4K の上限設定が甘かった**(2.4 参照) | 全表に行数上限を追加した |

---

## 4. P201 の判定

| 区分 | 件数 | 結果 |
| --- | --- | --- |
| 単体テスト | 410 | 全件 PASS |
| 結合テスト | 108 | 全件 PASS |
| 受け入れ結合テスト | 125(A006 の 15 件を含む) | **全件 PASS** |

**判定: 結合テスト・受け入れ結合テストにすべて合格した。P202 への差し戻しは行わない。**

* Reviewer Loop の終了条件(P201 で全件合格)を満たした。
* **P201 の判定は本 CR 対応の中で 1 回目**である。`SKILL-P903-refactor-execute.md` の定めにより、
  Refactor 経由の再入時は**対象 CR ごとに実行回数をリセットして数える**。停止条件(3 回)には該当しない。

---

## 5. 次工程への申し送り

| # | 事項 | 引き渡し先 |
| --- | --- | --- |
| 1 | **4K の判定をバイト数で代用している。** 実際のローカル LLM のトークナイザでの確認が必要 | `docs/P302-deliver.md` 10 章 |
| 2 | **原因候補ルールの読み替えの妥当性**(3 章の #1・#2)。実データでの確認が必要 | `docs/P302-deliver.md` 10 章 / P905 |
| 3 | **NFR-009(再現性)** — 既存の欠陥が CR-002 で解消される見込みだが、**A003 が 14 日規模でしか確認しておらず検証できていない** | `docs/P302-deliver.md` 10 章 |
| 4 | `report.md` を前提にした運用・スクリプトは**影響を受ける**(出力ファイル名が変わった) | `README.md` / `docs/P302-deliver.md` |
