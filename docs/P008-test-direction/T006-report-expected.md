あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T006 — レポート生成と正解の突き合わせ

## 【目的】

* 全モジュールを連結して `report.md` の**文字列を生成**し、`expected.json` の正解と突き合わせる。
* **本アプリの最終成果物である `report.md` が、後段の LLM が読める構造を保っている**ことを確認する(`docs/P002-frontend-spec.md` UI-04-00 / UI-04-02)。
* **プロセスの起動は行わない**(それは P009 の A-01 が担当する)。ここではモジュールを組み合わせて呼び、生成された文字列を検証する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 5.2 の T-06、4.4 TP-15 / TP-16、観点 F-17
* `docs/P002-frontend-spec.md` 4.1(全体構成)、4.2 UI-04-01 / UI-04-02、4.3(現象欄の定型文)、8.3 UI-08-03
* `docs/P001-requirement.md` FR-071 〜 FR-077、FR-113

## 【対象モジュール】

* `app/src/s_anomaly/reporter.py`
* 全モジュール(連結して呼ぶ)
* 対象スプリント: **U008**

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* `docs/P008-test-direction.md` 4.1 のビルド確認を実行し、成功していること。
* U002〜U008 の全タスクが `[x]` であること。
* **失敗した場合、`BLOCKED` として記録し本テストへ進まない。**

## 【使用するテストデータ】

* `app/tests/_work/normal/logs/`(読み取り専用)
* `app/tests/_work/normal/expected.json`(読み取り専用。**書き換え禁止**)
* `app/tests/integration/report_parser.py`(U008-T4 で作成済み。無ければ本テストで新規作成する)

## 【事前準備】

1. スイート初期化がこのスイートの実行で 1 回だけ済んでいること(T001 参照)。
2. T005 と同じ手順で、`Event` のリスト(相関・原因候補まで埋まった状態)を得る。
3. `render_report` に渡す `ctx` を組み立てる。**`now` には固定値 `datetime(2026, 6, 15, 10, 23, 45)` を渡す**(`docs/P006-test-plan.md` TP-11。現在時刻を暗黙に取らせない)。
4. **`write_report` は使わず、`render_report` の戻り値の文字列を検証する**(ファイル書き込みは T007 と P009 が担当する)。

## 【実行手順】

1. `reporter.render_report(ctx)` を呼び、Markdown 文字列を得る。
2. 構造を検証する(章・小節の存在)。
3. `report_parser` で文字列を解析し、イベントの一覧を得る。
4. `expected.json` を読み、`docs/P002-frontend-spec.md` UI-08-03 の 3 判定を行う。
5. 各イベント本文の 10 項目が漏れなく出力されていることを検証する。

## 【実行コマンド】

```
python -m unittest discover -s app/tests/integration -t app -p "test_report_expected.py" -v
```

* テストファイルは `app/tests/integration/test_report_expected.py` として**新規作成する**。

## 【期待結果】

**構造の確認**

| # | 確認項目 | 期待値 |
| --- | --- | --- |
| 1 | 見出し | 先頭が `# 環境分析レポート` |
| 2 | 章 | `## 1. 実行サマリ` / `## 2. 適用したアルゴリズム` / `## 3. 検知イベント` / `## 4. 実行時の注意事項` の **4 つすべて**が存在する |
| 3 | 小節 | `### 4.1` / `### 4.2` / `### 4.3` の **3 つすべて**が存在する |
| 4 | 1 章の内容 | 実行日時が `2026-06-15 10:23:45`(渡した固定値)。対象ディレクトリ・対象期間・ファイル数・総レコード数・系列数・脅威度別内訳が含まれる |
| 5 | 2 章の内容 | **10 行**(全アルゴリズム)。ID・名称・観点・状態・主なパラメータの列がある |
| 6 | 4 章の空欄処理 | 該当なしの小節に `(なし)` が書かれ、**小節自体が消えていない** |

**イベント本文の確認**

| # | 確認項目 | 期待値 |
| --- | --- | --- |
| 7 | イベント見出し | すべて `# イベントID( EVT-YYYYMMDD-NNN )` の書式(丸括弧の前後に半角スペース) |
| 8 | 項目の行頭 | すべて `* ` で始まる。**`( イベント種別` のような丸括弧始まりが 1 つも無い**(`docs/P001-requirement.md` 20章 #5 の確定解釈) |
| 9 | **項目の網羅** | **すべてのイベントに 9 項目**(イベント種別 / 脅威度 / データ / 値 / 開始 / 終了 / 現象 / 説明 / 原因候補 / 同時刻のその他のデータ)が存在する。**1 つも省略されていない**(UI-04-02) |
| 10 | 空の原因候補 | `causes` が空のイベントでは `* 原因候補 : 該当する候補なし (確度: —)` が出ている |
| 11 | 空の相関 | `correlations` が空のイベントでは `- (同時刻に有意な変化を示す他のデータはありません)` が出ている |
| 12 | 値の省略規則 | 値が 9 点を超えるイベントで、`…` を含み要素が 9 個であること |
| 13 | 現象欄 | 空文字でなく、`shape` に対応する定型文の特徴語(例: `floor_rise` なら「最低値が」)を含む |
| 14 | 並び順 | 脅威度降順・開始時刻昇順(FR-073) |

**正解との突き合わせ(UI-08-03)**

| # | 確認項目 | 期待値 |
| --- | --- | --- |
| 15 | **検知漏れ** | `injected` **7 件すべて**について、`expect_algorithms` のいずれかを「イベント種別」に含むイベントが、期間の重なり(前後 60 分の許容)を持って存在する |
| 16 | **誤検知** | `clean_series` に属するイベントが **0 件** |
| 17 | **脅威度** | 各 `injected` に対応するイベントの脅威度が `expect_min_severity` **以上** |
| 18 | 再現性 | 同じ `ctx` で 2 回 `render_report` を呼び、**文字列が完全一致** |

## 【合否判定基準】

* **PASS**: 上記 18 項目すべてが期待値と一致する。
* **FAIL**: 1 項目でも一致しない。
* **BLOCKED**: ビルド確認の失敗、または U002〜U008 のタスク未完了。
* **NOT RUN**: 実行環境の問題で実行自体ができなかった。

## 【失敗時に記録する内容】

* **失敗ログ抜粋**:
  * 構造の失敗(項目 1〜6)では、**生成された Markdown の先頭 100 行**を記録する。
  * 項目 9 の失敗では、**どのイベントのどの項目が欠けているか**を列挙する。
  * **項目 15(検知漏れ)の失敗では、どの `INJ-xxx` が拾えなかったか、そのイベントが 3 章に存在するか、存在するなら「イベント種別」に何が並んでいるかを書く。**
  * **項目 16(誤検知)の失敗では、`clean_series` のどのイベントが出たか、その「データ」「脅威度」「説明」欄を全文記録する。**
* **再現手順**: スイート初期化のコマンド、テストコマンド、渡した `now` の値。
* **影響がありそうなモジュール**:
  * 項目 1〜6、9〜13 の失敗 → `reporter.py`
  * 項目 14 の失敗 → `events.py`(`sort_for_report`)
  * 項目 15〜17 の失敗 → 検知器 / `events.py` / `reporter.py` / `report_parser.py` のいずれか。**`report_parser` の解析誤りである可能性を必ず確認してから、検知側の問題と結論すること**(T004 で `Detection` レベルの検知は確認済みであるため、T004 が PASS で T006 の項目 15 が FAIL なら、統合かレポート生成か解析のいずれかが原因である)
  * 項目 18 の失敗 → 順序が非決定的な箇所(`dict` の反復順、ソートキー不足)
* **テスト指示との矛盾の疑い**: `report.md` の書式が `docs/P002-frontend-spec.md` 4.2 と `docs/P001-requirement.md` 12章 で食い違うと判断した場合は「あり」とし該当箇所を書く。

## 【修正禁止事項】

* **アプリケーションコードを修正しない。**
* **`expected.json` を書き換えない**(改ざん)。
* **`report_parser.py` の解析を、失敗を回避する目的で緩めない。** ただし、`report_parser.py` 自体がまだ存在せず本テストで新規作成する場合は、新規作成そのものは修正に当たらない。
* **失敗したテストをスキップしない。**
* **項目 9(項目の網羅)を「軽微だから」として通してはならない。** 後段の LLM が項目の欠落を「異常がない」と誤読するため、これは仕様上の重大な欠陥である。
* 失敗は記録し、Reviewer Loop(P201〜)へ引き渡す。

## 【次タスクへ進む条件】

* 結果をテスト記録に残したこと。
* `docs/P008-test-direction.md` の T006 行を `[x]` に更新したこと。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。

---

> **P012 修正記録**: 矛盾点 #2 にもとづき、【実行コマンド】を `python -m unittest app.tests.*` 形式から `discover -t app` 形式へ改めた。詳細は `docs/P011-impact-analysis.md` 2.2 を参照。

---

## 実行結果 (P103)

**結果: PASS**。詳細は `docs/test-records/20260902-0521-test-record.md` の T006 の欄を参照。

---

# 【CR-001〜CR-004 による改訂】2026-09-05

**本テスト指示は、CR-001〜CR-004 の適用にともない次のとおり変更する。**
上記の本文のうち、下記と矛盾する記述は本節を正とする。

## 変更点(CR-001・CR-002・CR-004)

**単一の `report.md` ではなく、レポート群を対象にする。**

### 追加・変更する確認項目

1. **`report_{HOST}_{yyyymm}.md` が 1 つ以上生成されること。**
2. **`report_summary_{yyyymm}.md` が生成されること**(イベント 0 件でも)。
3. **`expected.json` との突き合わせは、全ホスト別ファイルを合わせた集合に対して行う**(P006 TP-15)。
   `report_summary_*.md` はイベント本文を持たないため、突き合わせの対象から除外する。
4. **分割の前後でイベントの総数が変わらないこと。** どのファイルにも現れないイベントが無いこと。
5. **イベント ID が `EVT-{YYYYMMDD}-{ホスト名}-{連番3桁}` の形式であること**(CR-003)。
6. **イベント本文の最終行が「同時に発生したアノマリー」であること**(観点1 のみのイベントの場合)。
7. **観点2 を含むイベントには「同時に発生したアノマリー」の項目行が無いこと。**
8. **サマリの「2. ホスト別の集計」の合計が、全ファイルのイベント実数と一致すること**(数え漏れ・二重計上がない)。
9. **各ホスト別ファイルの「1. このホストの集計」の件数が、そのファイル内のイベント実数と一致すること。**

### 更新が必要なテスト実装

* `app/tests/integration/report_parser.py` — **カレントディレクトリの `report_*.md` をすべて読む**ようにする。
  `report_summary_*.md` は除外する。
