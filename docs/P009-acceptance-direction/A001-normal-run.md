あなたはReviewer(受け入れ確認担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A001 — 正常系の通し実行と正解突き合わせ

## 【目的】

* **運用者が実際に行う使い方**(ログのディレクトリを指定して 1 回実行する)で、アプリが最初から最後まで動き、`report.md` が得られることを確認する。
* 生成テストデータに**意図的に埋め込んだ 7 種の異常**が、`report.md` の上で検知されていることを `expected.json` と突き合わせて確認する(`docs/P001-requirement.md` FR-113)。
* **本アプリの存在理由(膨大なログから異常を見つける)が果たされているかを判定する、最も重要なテストである。**

## 【対象モジュール】

* 全モジュール(プロセスとして起動する)

## 【前提条件】

* `docs/P009-acceptance-direction.md` 4.1 のビルド確認が成功していること。**失敗した場合は `BLOCKED` として記録し本テストへ進まない。**
* `docs/P008-test-direction.md` の全テスト(T001〜T008)が `[x]` であること。`[ ]` が残る場合は `BLOCKED` とする。

## 【使用するテストデータ】

* `app/tests/_work/normal/logs/`(読み取り専用)
* `app/tests/_work/normal/expected.json`(読み取り専用。**書き換え禁止**)

## 【事前準備】

1. スイート初期化を**このスイートの実行で 1 回だけ**行う(`docs/P009-acceptance-direction.md` 4.3)。
   ```
   python app/tests/integration/_setup_baseline.py
   ```
2. `tempfile.TemporaryDirectory` で作業ディレクトリを作る。**この中を `cwd` として起動する**(4.2)。
3. 作業ディレクトリに `settings.properties` を置かない(既定値で動作させる。`docs/P002-frontend-spec.md` UI-02-L01)。

## 【実行手順】

1. サブプロセスとしてアプリを起動する。
2. 終了コード・標準出力・標準エラーを取得する。
3. 作業ディレクトリの `report.md` を読む。
4. `report_parser`(`app/tests/integration/report_parser.py`)でイベント一覧を抽出する。
5. `expected.json` と突き合わせる(`docs/P002-frontend-spec.md` UI-08-03 の 3 判定)。

## 【実行コマンド】

```
python -m unittest discover -s app/tests/acceptance -t app -p "test_a001_normal_run.py" -v
```

* テストファイルは `app/tests/acceptance/test_a001_normal_run.py` として**新規作成する**。`app/tests/acceptance/__init__.py` も作る。
* テスト内で実行する実体は次である。
  ```
  python <repo>/app/s_anomaly.py <repo>/app/tests/_work/normal/logs
  ```

## 【期待結果】

| # | 確認項目 | 期待値 |
| --- | --- | --- |
| 1 | 終了コード | **0** |
| 2 | `report.md` の生成 | 作業ディレクトリに存在する |
| 3 | `report.md.tmp` | **存在しない** |
| 4 | 標準エラー | `Traceback` を含まない |
| 5 | 章構成 | `## 1.` 〜 `## 4.` と `### 4.1` 〜 `### 4.3` がすべて存在する |
| 6 | 1 章 | 対象ディレクトリ・対象期間(2026-06-01 〜 2026-06-14)・ファイル数・総レコード数・系列数・脅威度別内訳が含まれる |
| 7 | 2 章 | 10 個のアルゴリズムが全て列挙されている |
| 8 | 3 章 | イベントが 1 件以上ある |
| 9 | **検知漏れ** | `injected` **7 件すべて**について、`expect_algorithms` のいずれかを含むイベントが、期間の重なり(前後 60 分の許容)を持って存在する |
| 10 | **誤検知** | `clean_series` に属する**検知点がその系列の総点数の 1% 未満**であり、かつ **`clean_series` に属する SEVERE のイベントが 0 件**であること。根拠: `docs/P002-frontend-spec.md` **UI-08-03**(`docs/ADR.md` ADR-012 にもとづく)。**本欄は当初「イベントが 0 件」としていたが、これは達成不能であった。**4,000 点の系列に 2σ の判定を当てると正規分布のノイズだけで約 180 点が偶然閾値を超えるため、統計的手法である以上「0 件」にはならない。【実行手順】5 が当初から UI-08-03 を参照するよう指示していたとおり、UI-08-03 を正とする |
| 11 | **脅威度** | 各 `injected` に対応するイベントの脅威度が `expect_min_severity` **以上** |
| 12 | イベント本文 | すべてのイベントに 9 項目が漏れなくある(`docs/P002-frontend-spec.md` UI-04-02) |
| 13 | 文字コード・改行 | `report.md` が BOM なし UTF-8、改行が LF |
| 14 | 進捗ログ | 標準出力に `[S1]` 〜 `[S9]` がすべて現れる |

## 【合否判定基準】

* **PASS**: 上記 14 項目すべてが期待値と一致する。
* **FAIL**: 1 項目でも一致しない。
* **BLOCKED**: ビルド確認の失敗、または P008 のテストが未完了、またはベースラインの生成に失敗。
* **NOT RUN**: 実行環境の問題で実行自体ができなかった。

## 【失敗時に記録する内容】

* **失敗ログ抜粋**: 終了コード、標準エラーの全文、失敗した確認項目の期待値と実際の値。
  * **項目 9 の失敗では、どの `INJ-xxx` が拾えなかったか、その系列に対応するイベントが 3 章に存在するか、存在するなら「イベント種別」「脅威度」「開始」「終了」を全文記録する。**
  * **項目 10 の失敗では、`clean_series` に対して出たイベントの全文(10 項目すべて)を記録する。**
* **再現手順**: スイート初期化のコマンド、作業ディレクトリの作り方、起動コマンド。
* **影響がありそうなモジュール**: 項目 9/10/11 の失敗は、検知器・`events`・`reporter`・`report_parser` のいずれか。**T004(Detection レベル)と T006(レポート文字列レベル)の結果と突き合わせて、どの段階で落ちているかを特定して書く。**
* **テスト指示との矛盾の疑い**: 期待値が `docs/P001-requirement.md` / `docs/P002-frontend-spec.md` と食い違うと判断した場合は「あり」とし該当箇所を書く。

## 【修正禁止事項】

* **アプリケーションコードを修正しない。**
* **`expected.json` を書き換えない**(改ざん)。
* **検知されないからといって閾値・パラメータの既定値を変更しない。**
* **失敗したテストをスキップしない。**
* **同じ失敗に対して場当たり的な再テストを繰り返さない。**
* 失敗は記録し、**P202(修正計画)以降へ引き渡す。**

## 【次タスクへ進む条件】

* 結果をテスト記録に残したこと。
* `docs/P009-acceptance-direction.md` の A001 行を `[x]` に更新したこと。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。

---

> **P012 修正記録**: 矛盾点 #2 にもとづき、【実行コマンド】を `python -m unittest app.tests.*` 形式から `discover -t app` 形式へ改めた。詳細は `docs/P011-impact-analysis.md` 2.2 を参照。

---

> **P202/F002 修正記録**: 矛盾点 #1 にもとづき、【期待結果】項目 10 の誤検知の基準を `docs/P002-frontend-spec.md` UI-08-03(ADR-012)に合わせて訂正した。旧文言「イベントが 0 件」は統計的に達成不能であり、【実行手順】5 の参照指示とも食い違っていた。
> 詳細は `docs/P202-fix-plan/fixed/F002-test-direction-corrections.md` を参照。


---

# 【CR-001〜CR-004 による改訂】2026-09-05

**本テスト指示は、CR-001〜CR-004 の適用にともない次のとおり変更する。**
上記の本文のうち、下記と矛盾する記述は本節を正とする。

## 共通の変更点

**出力が単一の `report.md` から、次のファイル群に変わった**(P002 UI-04-F01)。

| ファイル名 | 内容 |
| --- | --- |
| `report_{HOST}_{yyyymm}.md` | ホスト × 年月ごとのイベント本文 |
| `report_summary_{yyyymm}.md` | 年月ごとの全ホスト横断サマリ |

* **本文中の「`report.md`」は、すべて「レポート群」に読み替える。**
* ファイルの存在確認は、**`report_*.md` のグロブ**で行う。
* 内容の確認は、**全ホスト別ファイルを合わせた集合**に対して行う。
* `app/tests/acceptance/_harness.py` の `report_path()` は **`report_paths()`(一覧を返す)に置き換える**。

## A001 固有の変更点

1. **`expected.json` との突き合わせは、全ホスト別ファイルを合わせた集合**に対して行う(P006 TP-15)。
2. **イベント ID が `EVT-{YYYYMMDD}-{ホスト名}-{連番3桁}` であること**を確認する(CR-003)。
3. **イベント本文の最終行**を確認する(CR-002)。
   * 観点1 のみのイベント: `* 同時に発生したアノマリー :` の行があり、`  - ` の明細が 1 行以上ある
   * 観点2 を含むイベント: **その項目行自体が無い**
4. **`report_summary_{yyyymm}.md` の集計値が、本文のイベント実数と一致すること**(CR-004)。
5. **サマリが 4K トークンに収まること**を確認する(CR-004)。判定はバイト数で代用してよい。
   目安は **UTF-8 で 4,000 バイト以下**とする。★FIXME★ トークン数の厳密な計測は、
   トークナイザが配布物に含まれないため行えない。バイト数による代用は Agent の想定である。
6. **各ホスト別ファイルの「1. このホストの集計」が 4K に収まること**(同上)。
