あなたはReviewer(受け入れ確認担当)です。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A007 — メモリ制限下での退避

## 【目的】

* `docs/P001-requirement.md` NFR-003「既定で最大 4GB。超過分は `temp_directory` へ退避して処理を継続する」を確認する。
* これは P000 第2章の明示要求「DuckDB インメモリ(+メモリからあふれたら temp ファイルを使う)」に直接対応する。**メモリを超えたら落ちるのではなく、退避して完走する**ことが要求である。

## 【対象モジュール】

* `app/src/s_anomaly/bootstrap.py`(`memory_limit` / `temp_directory` の適用)
* `app/src/s_anomaly/cli.py`(`StorageError` への変換)
* DuckDB(退避の実行主体)

## 【前提条件】

* `docs/P009-acceptance-direction.md` 4.1 のビルド確認が成功していること。**失敗した場合は `BLOCKED`。**
* A001 が PASS していること。

## 【使用するテストデータ】

* `app/tests/_work/normal/logs/`(読み取り専用)
* 可能であれば A006 で生成した大規模データ(退避が実際に起きやすいため)。無い場合は通常データで実施する。

## 【事前準備】

1. スイート初期化がこのスイートの実行で 1 回だけ済んでいること。
2. 一時作業ディレクトリを 3 つ作る(下記 3 ケース分。相互に影響させないため)。
3. **ケースごとに `app/` を一時ディレクトリへ複製し、複製側の `settings.properties` を次の内容に差し替える。**
   * **設定ファイルは `s_anomaly.py` と同じディレクトリから読まれる**(`docs/P002-frontend-spec.md` UI-02-L01)。当初の記述は「各作業ディレクトリに置く」であったが、そこに置いても読まれないため訂正した。
   * 複製には `app/tests/acceptance/_harness.py` の `make_dist()` を使ってよい。

   | ケース | `max_memory` | `temp_directory` |
   | --- | --- | --- |
   | ケース1(既定相当) | `4GB` | `./tmp` |
   | ケース2(退避を強制) | `256MB` | `./tmp` |
   | ケース3(極小) | `1KB` | `./tmp` |

## 【実行手順】

1. ケース1 で起動し、終了コードと `report.md` を取得する。
2. ケース2 で起動し、終了コード・`report.md`・**`tmp/` にファイルが作られたか**を確認する。
3. ケース3 で起動し、終了コードを確認する。
4. ケース1 とケース2 の `report.md` を、実行日時の行を除いて比較する。

## 【実行コマンド】

```
python -m unittest discover -s app/tests/acceptance -t app -p "test_a007_memory_spill.py" -v
```

* テストファイルは `app/tests/acceptance/test_a007_memory_spill.py` として**新規作成する**。

## 【期待結果】

| # | ケース | 確認項目 | 期待値 |
| --- | --- | --- | --- |
| 1 | 1 | 終了コード | **0** |
| 2 | 2 | 終了コード | **0**(退避して完走する。NFR-003) |
| 3 | 2 | `report.md` | 生成され、4 章構成を保つ |
| 4 | 2 | **結果の一致** | ケース1 と、実行日時の行を除いて**完全一致**(メモリ制限が結果を変えないこと) |
| 5 | 2 | `temp_directory` | 実行中または実行後に `tmp/` が作られている(退避の痕跡。**ファイルが残っていなくても、ディレクトリが作られていれば可**) |
| 6 | 2 | 標準エラー | `MemoryError` / `OutOfMemoryException` の未捕捉例外が**出ない** |
| 7 | 3 | 終了コード | **0 または 6**。DuckDB が最低限のメモリを確保できず失敗する場合は **6**(`StorageError`)であること。**99(想定外の例外)であってはならない** |
| 8 | 3 | 終了コード 6 のときのメッセージ | `max_memory` と `temp_directory` の**現在値**と見直しの案内が標準エラーに出る(`docs/P003-backend-spec.md` DS-01-04) |
| 9 | 全 | `temp_directory` の作成 | 存在しないパスを指定しても自動的に作られる(DS-14-04) |

## 【合否判定基準】

* **PASS**: 9 項目すべてが期待値と一致する。
* **FAIL**: 1 項目でも一致しない。**特に項目 7 で 99 が返る場合は、DuckDB 例外の捕捉漏れ(DS-01-04 の未実装)であり FAIL とする。**
* **BLOCKED**: ビルド確認の失敗、または A001 が FAIL。
* **NOT RUN**: 実行環境の問題で実行自体ができなかった。

## 【失敗時に記録する内容】

* **失敗ログ抜粋**: 各ケースの終了コードと標準エラー全文。項目 4 の失敗では `difflib` による差分。
* **再現手順**: 各ケースの `settings.properties` の内容、起動コマンド。
* **影響がありそうなモジュール**:
  * 項目 2・6 の失敗 → `bootstrap.py`(`SET temp_directory` が効いていない、または `SET memory_limit` の別名フォールバックが誤っている)
  * 項目 4 の失敗 → **結果がメモリ制限に依存している**。集計の順序が非決定的である疑い(`docs/P001-requirement.md` NFR-009 とも矛盾する)
  * 項目 7・8 の失敗 → `cli.py`(DS-01-04 の DuckDB 例外の捕捉と `StorageError` への変換)
  * 項目 9 の失敗 → `bootstrap.py`(`os.makedirs(..., exist_ok=True)`)
* **テスト指示との矛盾の疑い**: `max_memory = 1KB` が DuckDB に受理されない(設定値の検証で弾かれる)場合、期待値の設計が誤っている。この場合は「あり」とし、`docs/P002-frontend-spec.md` 2.2 の `max_memory` の許容範囲との関係を記録する。

## 【修正禁止事項】

* **アプリケーションコードを修正しない。**
* **`settings.properties` の値を、失敗を回避する目的で緩めない。**
* **失敗したテストをスキップしない。**
* 失敗は記録し、**P202 以降へ引き渡す。**

## 【次タスクへ進む条件】

* 結果をテスト記録に残したこと。
* `docs/P009-acceptance-direction.md` の A007 行を `[x]` に更新したこと。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、次のテストタスクに自動的に進んでください。

---

> **P012 修正記録**: 矛盾点 #2 にもとづき、【実行コマンド】を `python -m unittest app.tests.*` 形式から `discover -t app` 形式へ改めた。詳細は `docs/P011-impact-analysis.md` 2.2 を参照。

---

> **P202/F002 修正記録**: 矛盾点 #4 にもとづき、【事前準備】3 の `settings.properties` の置き場所を UI-02-L01 に合わせて訂正した。確認する内容(終了コード 0 / 6、退避しての完走、結果の一致)は変えていない。
> 詳細は `docs/P202-fix-plan/fixed/F002-test-direction-corrections.md` を参照。


---

# 【CR-001〜CR-004 による改訂】2026-09-05

**本テスト指示は、CR-001〜CR-004 の適用にともない次のとおり変更する。**
上記の本文のうち、下記と矛盾する記述は本節を正とする。

## 共通の変更点

**出力が単一の `report.md` から、次のファイル群に変わった**(P002 UI-04-F01)。

| ファイル名 | 内容 |
| --- | --- |
| `report_{HOST}_{yyyymm}.md` | ホスト × 年月ごとのイベント本文 |
| `report_summary_{yyyymm}.md` | 年月ごとの全ホスト横断サマリ |

* **本文中の「`report.md`」は、すべて「レポート群」に読み替える。**
* ファイルの存在確認は、**`report_*.md` のグロブ**で行う。
* 内容の確認は、**全ホスト別ファイルを合わせた集合**に対して行う。
* `app/tests/acceptance/_harness.py` の `report_path()` は **`report_paths()`(一覧を返す)に置き換える**。

## 固有の変更点

**本テストは `report.md` の存在・内容を確認しているため、レポート群に読み替える。**
確認する内容そのものは変わらない。
