あなたはReviewer(受け入れ確認担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A002 — 終了コード 9 種の再現

## 【目的】

* `docs/P002-frontend-spec.md` 5章 UI-05 が定めた **9 種の終了コードすべて**を、**プロセスとして実際に発生させて**確認する。
* 終了コードは**運用者がスクリプトの成否を判定する唯一の契約**であり(`docs/P001-requirement.md` FR-090)、単体テストの `main()` の戻り値だけでなく、プロセスの終了ステータスとして正しく伝わることを確かめる必要がある。
* あわせて、各コードで**標準エラーに出るメッセージが切り分けに足るか**を確認する。

## 【対象モジュール】

* `app/s_anomaly.py`(`sys.exit` への受け渡し)
* `app/src/s_anomaly/cli.py`、`errors.py`
* 全モジュール(エラーの発生源として)

## 【前提条件】

* `docs/P009-acceptance-direction.md` 4.1 のビルド確認が成功していること。**失敗した場合は `BLOCKED`。**
* P008 の全テストが `[x]` であること。

## 【使用するテストデータ】

| 終了コード | 用意するもの |
| --- | --- |
| 0 | `app/tests/_work/normal/logs/` |
| 1 | 引数なし / 存在しないパス / ファイルのパス |
| 2 | **空のディレクトリ**(一時ディレクトリに作る) |
| 3 | **不正な `settings.properties`**(`ALG-A4.lambda = 1.5` など範囲外の値)。**置き場所は `s_anomaly.py` と同じディレクトリである**(`docs/P002-frontend-spec.md` UI-02-L01)。作業ディレクトリに置いても読まれない。`app/` を一時ディレクトリへ複製し、複製側の設定を差し替える |
| 4 | **全ファイルが破損したディレクトリ**(見出しのみ・列数不一致のみで有効行 0 のファイル群) |
| 5 | 正常なログ + **書き込みできないカレントディレクトリ** |
| 6 | 正常なログ + `settings.properties` に `max_memory = 1KB`。**置き場所は上記 3 と同じく `s_anomaly.py` と同じディレクトリ**(UI-02-L01) |
| 7 | 正常なログ + **DuckDB を読み込めない状態** |
| 99 | 想定外の例外を起こす状態 |

## 【事前準備】

1. スイート初期化がこのスイートの実行で 1 回だけ済んでいること。
2. **終了コードごとに独立した一時ディレクトリ**を作る(相互に影響させないため)。
3. 終了コード 4 用のデータは、`app/tests/_work/broken/` をそのまま使わず、**有効行が 1 行も無いファイルだけを集めた専用ディレクトリ**を作る(`_work/broken` は有効行を含むため終了コード 0 になる。A009 で使う)。
4. 終了コード 5 の再現方法は OS 依存が大きいため、次の順で試す。
   * (a) 作業ディレクトリに `report.md` という名前の**ディレクトリ**を作る(ファイルとして開けなくなる)。
   * (b) (a) で再現しない場合は、読み取り専用属性を付与する。
   * (c) いずれも再現しない場合は `NOT RUN` とせず、**モジュールを直接呼ぶ形で `os.replace` を失敗させる補助テスト**を実施し、その旨を記録する(T007 で確認済みであることも併記する)。
5. 終了コード 7 の再現方法: **`app/vendor/` に実行環境のタグと一致するディレクトリを作り、その中に壊れた `duckdb` パッケージ(`duckdb/__init__.py` に `raise ImportError`)を置く。加えて、`PYTHONPATH` に同じく壊れた `duckdb` パッケージを置いて、環境側の DuckDB も隠す。**
   * **★ACCEPTED★ 当初は「vendor に壊れたパッケージを置けば `sys.path` の先頭に入るため環境の DuckDB も隠れる」としていたが、これは誤りであった。** `docs/P003-backend-spec.md` **DS-14-02** のとおり、実装は vendor の読み込みに失敗すると `sys.path` を元に戻してから**環境の DuckDB へフォールバックする**。これは意図された正しい挙動であり、アプリの欠陥ではない。したがって環境側も隠さなければ終了コード 7 は再現しない。残存リスク: なし(P201 の実測で再現を確認済み)。
   * **あわせて「壊れた vendor だけでは終了コード 0 のまま(フォールバックが働く)」ことも確認する**(`app/tests/acceptance/test_a002_exit_codes.py` の `test_11b_vendor_fallback_alone_does_not_fail`)。
6. 終了コード 99 の再現方法: **`PYTHONSTARTUP` などでは制御できない**ため、`app/settings.properties` を一時的に壊すのではなく、**環境変数 `S_ANOMALY_FORCE_ERROR` のような仕掛けを実装に入れることはしない**。代わりに、**`app/src/s_anomaly/` にモンキーパッチを当てたラッパースクリプトを一時ディレクトリに作り、それをサブプロセスで起動する**。★ACCEPTED★ 検討: 実装に再現用のフック(環境変数など)を入れる案。**不採用**。テストのためのフックを製品コードに残すことになり、閉域環境で誤って踏まれる余地を作るため。**P201 の実測で、モンキーパッチを当てたラッパースクリプト方式で終了コード 99 を再現できることを確認した**(`app/tests/acceptance/test_a002_exit_codes.py` の `WRAPPER` と `test_12_unexpected_exception_returns_99`)。残存リスク: ラッパーがパッチする対象(`metrics.build_metrics`)を実装側で改名すると、テストが「例外を注入できないまま成功」しうる。ラッパーは Traceback の出力も確認することでこれを検出する。

## 【実行手順】

9 種それぞれについて、次を行う。

1. 該当する一時ディレクトリと入力を用意する。
2. サブプロセスとして起動する。
3. `returncode` / `stdout` / `stderr` を取得する。
4. 期待値表と突き合わせる。

## 【実行コマンド】

```
python -m unittest discover -s app/tests/acceptance -t app -p "test_a002_exit_codes.py" -v
```

* テストファイルは `app/tests/acceptance/test_a002_exit_codes.py` として**新規作成する**。

## 【期待結果】

| # | 終了コード | 期待する `returncode` | 標準エラーに含まれるべき内容 | `report.md` |
| --- | --- | --- | --- | --- |
| 1 | 正常終了 | **0** | (Traceback を含まない) | **生成される** |
| 2 | 引数 0 個 | **1** | usage(`usage: s_anomaly.py <dir>`) | 生成されない |
| 3 | 引数 2 個以上 | **1** | 「引数は 1 個です」と受け取った引数の列挙 | 生成されない |
| 4 | 存在しないパス | **1** | 「存在しません」と受け取ったパス | 生成されない |
| 5 | ファイルを指定 | **1** | 「ディレクトリではありません」 | 生成されない |
| 6 | 対象 0 件 | **2** | 探索したパスと対象ファイル名パターン 3 種 | 生成されない |
| 7 | 設定不正 | **3** | 該当セクション・キー・与えられた値・期待する型/範囲 | 生成されない |
| 8 | 全解析失敗 | **4** | ファイルごとの失敗理由 | 生成されない |
| 9 | 書き込み失敗 | **5** | 理由。**加えて標準出力にレポート全文**(UI-05-01) | 生成されない(または壊れていない) |
| 10 | メモリ不足 | **6** | 理由と `max_memory` / `temp_directory` の見直し案内 | 生成されない |
| 11 | vendor 不一致 | **7** | 期待した vendor パス・実行環境(OS/arch/Python 版)・用意されている vendor 一覧・README への案内 | 生成されない |
| 12 | 想定外の例外 | **99** | `Traceback` | 生成されない |
| 13 | 全アルゴリズム `false` | **0** | WARNING(エラーではない) | **生成される**(3 章が「検知された異常はありません。」) |

**補足**: 項目 10(終了コード 6)は `max_memory = 1KB` で必ず再現するとは限らない。再現しない場合は、より小さい値または大量データで試し、それでも再現しなければ**その事実を記録したうえで、モジュールを直接呼ぶ補助テスト**(`duckdb` の例外を注入)の結果をもって代替とする。**「再現できなかった」を黙って PASS にしてはならない。**

## 【合否判定基準】

* **PASS**: 13 項目すべてが期待値と一致する。
* **FAIL**: 1 項目でも一致しない(補助テストで代替した項目は、代替した旨を記録したうえで、その補助テストが合格していれば PASS 相当として扱ってよい)。
* **BLOCKED**: ビルド確認の失敗、または P008 のテストが未完了。
* **NOT RUN**: 実行環境の制約で再現方法自体が成立しなかった項目がある場合、**その項目のみ** `NOT RUN` として個別に記録し、テスト全体の結果は残りの項目で判定する。

## 【失敗時に記録する内容】

* **失敗ログ抜粋**: 期待した終了コードと実際の終了コード、標準エラーの全文。
* **再現手順**: 各終了コードの再現に使った入力(ディレクトリ構造、`settings.properties` の内容)。
* **影響がありそうなモジュール**:
  * 期待と異なる終了コードが返る → `cli.py`(例外の捕捉と `exit_code` の対応)、`errors.py`
  * 終了コードは正しいがメッセージが不足 → 該当する例外の `user_message()`
  * 項目 9 でレポート全文が標準出力に出ない → `cli.py`(UI-05-01 の実装漏れ)
* **テスト指示との矛盾の疑い**: 【事前準備】の再現方法が成立しないことによる失敗は、**アプリの欠陥ではなくテスト指示の問題**である。この場合は「あり」とし、その旨を明記する。なお、当初 ★FIXME★ を付していた項目 5(終了コード 7)と項目 6(終了コード 99)は、**P201 の実測を経ていずれも ★ACCEPTED★ として解消済みである**(P202/F002)。

## 【修正禁止事項】

* **アプリケーションコードを修正しない。** 終了コードを再現しやすくするためのフック(環境変数など)を実装に追加してはならない。
* **テストコードをその場で都合よく変更しない**(新規作成は除く)。
* **失敗したテストをスキップしない。**
* **再現できない項目を、期待値を変えて成功扱いにしない。** `NOT RUN` として正直に記録する。
* 失敗は記録し、**P202 以降へ引き渡す。**

## 【次タスクへ進む条件】

* 結果をテスト記録に残したこと。**個別に `NOT RUN` とした項目があれば、その項目と理由を明記すること。**
* `docs/P009-acceptance-direction.md` の A002 行を `[x]` に更新したこと。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。

---

> **P012 修正記録**: 矛盾点 #2 にもとづき、【実行コマンド】を `python -m unittest app.tests.*` 形式から `discover -t app` 形式へ改めた。詳細は `docs/P011-impact-analysis.md` 2.2 を参照。

---

> **P202/F002 修正記録**: 矛盾点 #2・#5 にもとづき、(a) 終了コード 3 / 6 の `settings.properties` の置き場所を UI-02-L01 に合わせて明記し、(b)【事前準備】5 の終了コード 7 の再現方法を DS-14-02 の実挙動に合わせて訂正した。★FIXME★ の懸念(環境の DuckDB が優先される)が的中したため、これを ★ACCEPTED★ として解消した。
> 詳細は `docs/P202-fix-plan/fixed/F002-test-direction-corrections.md` を参照。
> **追加**: F002 の対象 6 件には含めていなかったが、【事前準備】6(終了コード 99 の再現方法)の ★FIXME★ も、P201 の実測で再現できていたため★ACCEPTED★ として解消した。あわせて【失敗時に記録する内容】の「★FIXME★ を付した項目 5・7・99」という記述が古くなったため更新した。**いずれもアプリケーションコードは変更していない。**


---

# 【CR-001〜CR-004 による改訂】2026-09-05

**本テスト指示は、CR-001〜CR-004 の適用にともない次のとおり変更する。**
上記の本文のうち、下記と矛盾する記述は本節を正とする。

## 共通の変更点

**出力が単一の `report.md` から、次のファイル群に変わった**(P002 UI-04-F01)。

| ファイル名 | 内容 |
| --- | --- |
| `report_{HOST}_{yyyymm}.md` | ホスト × 年月ごとのイベント本文 |
| `report_summary_{yyyymm}.md` | 年月ごとの全ホスト横断サマリ |

* **本文中の「`report.md`」は、すべて「レポート群」に読み替える。**
* ファイルの存在確認は、**`report_*.md` のグロブ**で行う。
* 内容の確認は、**全ホスト別ファイルを合わせた集合**に対して行う。
* `app/tests/acceptance/_harness.py` の `report_path()` は **`report_paths()`(一覧を返す)に置き換える**。

## 固有の変更点

**本テストは `report.md` の存在・内容を確認しているため、レポート群に読み替える。**
確認する内容そのものは変わらない。
