あなたはReviewer(受け入れ確認担当)です。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A009 — 破損データでの完走

## 【目的】

* `docs/P001-requirement.md` FR-014「解析できない行はその行だけを読み飛ばし、件数を集計して報告する」と FR-091「一部のファイルの解析に失敗しても、有効なレコードが 1 件でも得られた場合は処理を継続し、終了コード 0 とする」を確認する。
* **運用ログは実際に壊れる**(採取プロセスの異常終了、ディスク満杯、ローテーション中のコピー)。1 行の破損で数百ファイルの解析全体が失敗する実装は、運用に耐えない。

## 【対象モジュール】

* `app/src/s_anomaly/loaders/*`
* `app/src/s_anomaly/cli.py`(FR-091 の判定)
* `app/src/s_anomaly/reporter.py`(4.1 節への集計出力)

## 【前提条件】

* `docs/P009-acceptance-direction.md` 4.1 のビルド確認が成功していること。**失敗した場合は `BLOCKED`。**

## 【使用するテストデータ】

* `app/tests/_work/broken/logs/`(スイート初期化で生成済み。読み取り専用)。`gen_testdata.py --broken` が作る 9 ファイル。
* `app/tools/gen_testdata.py` の `BROKEN_EXPECTATIONS` 定数(期待値の出所)
* **加えて、正常データと破損データを混在させたディレクトリ**を一時的に組み立てる(FR-091 の「一部が壊れていても他が読める」を確認するため)。

## 【事前準備】

1. スイート初期化がこのスイートの実行で 1 回だけ済んでいること。
2. 一時作業ディレクトリを 2 つ作る。
   * ケース1: 入力 = `app/tests/_work/broken/logs/`(破損のみ。ただし有効行を含むファイルがあるため終了コードは 0 になるはず)
   * ケース2: 入力 = 一時ディレクトリに、`app/tests/_work/normal/logs/` の一部(3 ファイル程度)と `app/tests/_work/broken/logs/` の全部をコピーした混在ディレクトリ
3. **`app/tests/_work/` へは書き込まない**(コピー元として読むだけ)。

## 【実行手順】

1. ケース1 でサブプロセス起動する。
2. ケース2 でサブプロセス起動する。
3. 両方の終了コード・標準エラー・`report.md` を取得する。
4. `report.md` の 4.1 節(読み飛ばした行)を解析し、`BROKEN_EXPECTATIONS` と突き合わせる。

## 【実行コマンド】

```
python -m unittest discover -s app/tests/acceptance -t app -p "test_a009_broken_input.py" -v
```

* テストファイルは `app/tests/acceptance/test_a009_broken_input.py` として**新規作成する**。

## 【期待結果】

| # | ケース | 確認項目 | 期待値 |
| --- | --- | --- | --- |
| 1 | 1 | 終了コード | **0**(有効レコードが 1 件以上あるため。FR-091) |
| 2 | 1 | `report.md` | 生成され、4 章構成を保つ |
| 3 | 1 | 標準エラーの WARNING | 破損したファイルについて、**ファイル名・理由・件数**が出ている |
| 4 | 1 | 標準エラー | 未捕捉の `Traceback` を**含まない** |
| 5 | 1 | **4.1 節の内容** | `BROKEN_EXPECTATIONS` の期待値と、**ファイルごとの理由コードと件数が一致**する |
| 6 | 1 | 理由コードの種類 | 4 種(`列数不一致` / `日付書式不正` / `数値変換不能` / `見出し不明`)のいずれかのみ |
| 7 | 2 | 終了コード | **0** |
| 8 | 2 | **正常データの処理** | 混在させた正常ファイル由来のレコードが `report.md` の 1 章の総レコード数に反映されている(破損ファイルの存在で正常データが失われていない) |
| 9 | 2 | 4.1 節 | 破損ファイルの記録が載る一方、正常ファイルは載らない |
| 10 | 両 | BOM 付き・CRLF のファイル | **エラーとして記録されない**(正常に読めること。`docs/P001-requirement.md` NFR-007) |
| 11 | 両 | 空ファイル・見出しのみのファイル | エラーとして記録されない(`ok_rows == 0` だが異常ではない) |

## 【合否判定基準】

* **PASS**: 11 項目すべてが期待値と一致する。
* **FAIL**: 1 項目でも一致しない。**特に項目 1・7(終了コード 0)が満たされない場合、FR-091 の未実装であり FAIL とする。**
* **BLOCKED**: ビルド確認の失敗、またはベースラインの生成に失敗。
* **NOT RUN**: 実行環境の問題で実行自体ができなかった。

## 【失敗時に記録する内容】

* **失敗ログ抜粋**: 終了コード、標準エラー全文、`report.md` の 4.1 節の全文。
  * 項目 5 の失敗では、**どのファイルのどの理由コードが何件ずれたか**を表形式で記録する。
* **再現手順**: 混在ディレクトリの構成(どのファイルをコピーしたか)、起動コマンド。
* **影響がありそうなモジュール**:
  * 項目 1・7 の失敗 → `cli.py`(DS-01-03 の「全テーブルの合計有効レコード数が 0」の判定が誤っている)
  * 項目 3・5 の失敗 → 該当するローダ、または `loaders/__init__.py` の `record_errors`、または `reporter.py`(4.1 節の生成)
  * 項目 4 の失敗 → 破損行で例外が送出されている。FR-014「その行だけを読み飛ばす」の未実装
  * 項目 10 の失敗 → `loaders/__init__.py` の `open_text`(`utf-8-sig` / ユニバーサル改行)
* **テスト指示との矛盾の疑い**: `BROKEN_EXPECTATIONS` の定義が `docs/P003-backend-spec.md` DS-LD-05 と矛盾すると判断した場合は「あり」とし該当箇所を書く。

## 【修正禁止事項】

* **アプリケーションコードを修正しない。**
* **`BROKEN_EXPECTATIONS` を、失敗を回避する目的で書き換えない**(改ざん)。
* **`app/tests/_work/broken/` の生成物を手で編集しない。**
* **失敗したテストをスキップしない。**
* 失敗は記録し、**P202 以降へ引き渡す。**

## 【次タスクへ進む条件】

* 結果をテスト記録に残したこと。
* `docs/P009-acceptance-direction.md` の A009 行を `[x]` に更新したこと。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、次のテストタスクに自動的に進んでください。

---

> **P012 修正記録**: 矛盾点 #2 にもとづき、【実行コマンド】を `python -m unittest app.tests.*` 形式から `discover -t app` 形式へ改めた。詳細は `docs/P011-impact-analysis.md` 2.2 を参照。


---

# 【CR-001〜CR-004 による改訂】2026-09-05

**本テスト指示は、CR-001〜CR-004 の適用にともない次のとおり変更する。**
上記の本文のうち、下記と矛盾する記述は本節を正とする。

## 共通の変更点

**出力が単一の `report.md` から、次のファイル群に変わった**(P002 UI-04-F01)。

| ファイル名 | 内容 |
| --- | --- |
| `report_{HOST}_{yyyymm}.md` | ホスト × 年月ごとのイベント本文 |
| `report_summary_{yyyymm}.md` | 年月ごとの全ホスト横断サマリ |

* **本文中の「`report.md`」は、すべて「レポート群」に読み替える。**
* ファイルの存在確認は、**`report_*.md` のグロブ**で行う。
* 内容の確認は、**全ホスト別ファイルを合わせた集合**に対して行う。
* `app/tests/acceptance/_harness.py` の `report_path()` は **`report_paths()`(一覧を返す)に置き換える**。

## 固有の変更点

**本テストは `report.md` の存在・内容を確認しているため、レポート群に読み替える。**
確認する内容そのものは変わらない。
