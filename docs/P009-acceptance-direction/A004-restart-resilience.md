あなたはReviewer(受け入れ確認担当)です。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A004 — 再起動耐性

## 【目的】

* **アプリケーションを停止・再起動しても正常に起動すること**を確認する(`docs/P006-test-plan.md` 2.3 の O-01 / O-02、`SKILL-P003-backend-spec.md` の要求)。
* 本アプリは永続化されたデータストアを持たないため、**スキーマ適用は自明に冪等**である(`docs/P002-frontend-spec.md` 6.4)。しかし「自明である」という主張だけで済ませず、**実際に 2 回連続で実行して確認する**。
* 本アプリでの実質的な論点は**ファイルシステム上の残骸**である。前回の `report.md`・`report.md.tmp`・`temp_directory` の一時ファイルが残った状態で 2 回目を実行し、1 回目と同じ結果になることを確認する。

> **本テストの特殊性**: `SKILL-P009-acceptance-direction.md` が指摘するとおり、**残骸が残っていること自体が前提**であるため、1 回目と 2 回目の間にベースライン復元を挟んではならない。本テストは専用の一時ディレクトリの中で 1 回目と 2 回目を完結させ、他のテストとデータを共有しない(`docs/P009-acceptance-direction.md` 4.3 の注記)。

## 【対象モジュール】

* 全モジュール(特に `bootstrap`(`temp_directory` の扱い)、`schema`(DDL の冪等性)、`reporter`(上書き))

## 【前提条件】

* `docs/P009-acceptance-direction.md` 4.1 のビルド確認が成功していること。**失敗した場合は `BLOCKED`。**

## 【使用するテストデータ】

* `app/tests/_work/normal/logs/`(読み取り専用)

## 【事前準備】

1. スイート初期化がこのスイートの実行で 1 回だけ済んでいること。**本テストの 1 回目と 2 回目の間には実行しない。**
2. `tempfile.TemporaryDirectory` で**本テスト専用**の作業ディレクトリを 1 つ作る。1 回目も 2 回目もこの同じディレクトリを `cwd` として起動する。
3. **`settings.properties` は置かなくてよい(既定値で足りる)。**
   * 本項の意図は「残骸の確認対象を作業ディレクトリ内に閉じる」ことである。**`temp_directory` の既定値がそもそも `./tmp`(作業ディレクトリからの相対)であるため、既定のままで意図は満たされる。**
   * なお、設定を変える必要が生じた場合、**置き場所は作業ディレクトリではなく `s_anomaly.py` と同じディレクトリである**(`docs/P002-frontend-spec.md` UI-02-L01)。当初の記述は作業ディレクトリに置けば読まれるかのように読めたが、それは誤りである。

## 【実行手順】

1. **1 回目**: サブプロセス起動する。終了コードと `report.md` を保存する。
2. 作業ディレクトリの状態を記録する(`report.md` の存在、`tmp/` の中身)。
3. **意図的に残骸を作る**: `report.md.tmp` という名前のファイルを、壊れた内容(`### 途中で切れたレポート`)で作る。
4. **2 回目**: 同じ作業ディレクトリで、同じコマンドを起動する。
5. 終了コードと `report.md` を取得する。
6. **3 回目**: さらにもう 1 回同じ条件で起動する(2 回で偶然通る場合を排除する)。

## 【実行コマンド】

```
python -m unittest discover -s app/tests/acceptance -t app -p "test_a004_restart.py" -v
```

* テストファイルは `app/tests/acceptance/test_a004_restart.py` として**新規作成する**。

## 【期待結果】

| # | 確認項目 | 期待値 |
| --- | --- | --- |
| 1 | 1 回目の終了コード | **0** |
| 2 | **2 回目の終了コード** | **0**(残骸があっても正常起動する。O-01) |
| 3 | 3 回目の終了コード | **0** |
| 4 | **`report.md` の一致** | 1 回目・2 回目・3 回目で、実行日時の行を除いて**完全一致**(O-02) |
| 5 | `report.md.tmp` | **3 回とも、実行後に存在しない**(手で作った残骸が掃除されている。O-03) |
| 6 | 2 回目の標準エラー | `Traceback` を含まない。特に `duplicate` / `already exists` 系の DDL エラーが**出ない**(スキーマ適用の冪等性) |
| 7 | `tmp/` の残骸 | 2 回目の実行が `tmp/` の残骸で失敗しない |
| 8 | イベント ID | 3 回とも同一 |
| 9 | 検知件数 | 3 回とも同一 |

## 【合否判定基準】

* **PASS**: 9 項目すべてが期待値と一致する。
* **FAIL**: 1 項目でも一致しない。**特に「2 回目だけ失敗する」場合は、`docs/P006-test-plan.md` が最も警戒している事象であり、必ず FAIL とする。**
* **BLOCKED**: ビルド確認の失敗、またはベースラインの生成に失敗。
* **NOT RUN**: 実行環境の問題で実行自体ができなかった。

## 【失敗時に記録する内容】

* **失敗ログ抜粋**: 各回の終了コードと標準エラーの全文。項目 4 の失敗では `difflib` による差分。
* **再現手順**: 作業ディレクトリの作り方、残骸の作り方(`report.md.tmp` の内容)、3 回の起動コマンド。
* **影響がありそうなモジュール**:
  * 項目 2・6 の失敗 → `schema.py`(`CREATE TABLE IF NOT EXISTS` になっていない)、`bootstrap.py`(`temp_directory` の作成が既存ディレクトリで失敗する)
  * 項目 4 の失敗 → 前回の出力が入力に混入している疑い。**`report.md` を `{dir}` 配下に置いていないか**、`discovery` が `report.md` を拾っていないかを確認する
  * 項目 5 の失敗 → `reporter.py`(`try/finally` での一時ファイル削除)
  * 項目 7 の失敗 → `bootstrap.py`(`temp_directory` の初期化)
* **テスト指示との矛盾の疑い**: 期待値が `docs/P002-frontend-spec.md` 6.4 / `docs/P006-test-plan.md` 2.3 と食い違うと判断した場合は「あり」とし該当箇所を書く。

## 【修正禁止事項】

* **アプリケーションコードを修正しない。**
* **1 回目と 2 回目の間に、作業ディレクトリを掃除しない。** 残骸が残っていることが本テストの前提である。
* **テストコードをその場で都合よく変更しない**(新規作成は除く)。
* **失敗したテストをスキップしない。**
* 失敗は記録し、**P202 以降へ引き渡す。**

## 【次タスクへ進む条件】

* 結果をテスト記録に残したこと。
* `docs/P009-acceptance-direction.md` の A004 行を `[x]` に更新したこと。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、次のテストタスクに自動的に進んでください。

---

> **P012 修正記録**: 矛盾点 #2 にもとづき、【実行コマンド】を `python -m unittest app.tests.*` 形式から `discover -t app` 形式へ改めた。詳細は `docs/P011-impact-analysis.md` 2.2 を参照。

---

> **P202/F002 修正記録**: 矛盾点 #3 にもとづき、【事前準備】3 を訂正した。設定ファイルは `s_anomaly.py` と同じディレクトリから読まれる(UI-02-L01)。本テストの意図は `temp_directory` の既定値 `./tmp` で満たされるため、設定ファイルは不要である。
> 詳細は `docs/P202-fix-plan/fixed/F002-test-direction-corrections.md` を参照。


---

# 【CR-001〜CR-004 による改訂】2026-09-05

**本テスト指示は、CR-001〜CR-004 の適用にともない次のとおり変更する。**
上記の本文のうち、下記と矛盾する記述は本節を正とする。

## 共通の変更点

**出力が単一の `report.md` から、次のファイル群に変わった**(P002 UI-04-F01)。

| ファイル名 | 内容 |
| --- | --- |
| `report_{HOST}_{yyyymm}.md` | ホスト × 年月ごとのイベント本文 |
| `report_summary_{yyyymm}.md` | 年月ごとの全ホスト横断サマリ |

* **本文中の「`report.md`」は、すべて「レポート群」に読み替える。**
* ファイルの存在確認は、**`report_*.md` のグロブ**で行う。
* 内容の確認は、**全ホスト別ファイルを合わせた集合**に対して行う。
* `app/tests/acceptance/_harness.py` の `report_path()` は **`report_paths()`(一覧を返す)に置き換える**。

## A004 固有の変更点

1. **前回実行時の `report_*.md` が残った状態**で 2 回目を実行し、1 回目と同じ結果になることを確認する。
2. **★CR-001 による新しい確認項目**: 前回はイベントがあったホストが今回 0 件だった場合、
   **そのホストの前回のファイルが残る**(P002 UI-04-05)。**これは仕様どおりの挙動であり、欠陥ではない。**
   この状態を作り、**実行が失敗しないこと**を確認する。
3. `tmp/` の残骸に対する確認は従来どおり。
