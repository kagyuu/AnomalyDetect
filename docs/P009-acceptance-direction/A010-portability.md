あなたはReviewer(受け入れ確認担当)です。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A010 — 配布物としての可搬性

## 【目的】

* `docs/P001-requirement.md` NFR-011「配布資産をコピーするだけで動作すること。インストーラを要求しない」と FR-001 / FR-002 を確認する。
* P000 第2章の要求「実行環境はインターネットに接続されていない環境のため `pip install` はできません。このアプリケーションの配布資産一式を持っていけば、そのまま動くようにしてください」に直接対応する、**受け入れ観点として最も重要なテストの 1 つ**である。
* あわせて、`vendor/` が実行環境と一致しないときに、**終了コード 7 と切り分けに足る情報**が出ることを確認する(`docs/P003-backend-spec.md` DS-14-03)。

## 【対象モジュール】

* `app/s_anomaly.py`(`sys.path` の解決)
* `app/src/s_anomaly/bootstrap.py`(`vendor/` の解決、終了コード 7)
* `app/` 全体(コピー単位としての完結性)

## 【前提条件】

* `docs/P009-acceptance-direction.md` 4.1 のビルド確認が成功していること。**失敗した場合は `BLOCKED`。**
* A001 が PASS していること。

## 【使用するテストデータ】

* `app/tests/_work/normal/logs/`(読み取り専用)
* `app/` ディレクトリ一式(コピー対象)

## 【事前準備】

1. スイート初期化がこのスイートの実行で 1 回だけ済んでいること。
2. `tempfile.TemporaryDirectory` に**リポジトリの外**を模した領域を作る。
3. **`app/` を、`tests/_work/` と `__pycache__` を除いてコピーする**(`shutil.copytree` の `ignore` を使う)。コピー先を `{tmp}/dist/app/` とする。
4. 入力データは、リポジトリ側の `app/tests/_work/normal/logs/` を参照する(データはアプリの配布物ではないため)。

> **本テストの制約** ★ACCEPTED★
> `vendor/` の実体(DuckDB の wheel)は、配布先の Python 版が未確定であるため本プロジェクトでは用意していない(`docs/P005-impl-plan.md` 7章、`docs/P007-impl-direction.md` 5章 #6)。したがって本テストでは **(a) 環境の DuckDB へのフォールバック経路での動作**と **(b) `vendor/` が不一致のときの終了コード 7 とメッセージ**を確認する。**`vendor/` からの実際の読み込みは確認できない。** 残存リスク: 閉域環境で `vendor/` から読む経路が未検証のまま納品される。**P302 の実行前チェックで必ず確認する。**

## 【実行手順】

**ケース A: コピー先での実行(フォールバック経路)**

1. `{tmp}/dist/app/` を起点として、別の一時作業ディレクトリを `cwd` にサブプロセス起動する。
   ```
   python {tmp}/dist/app/s_anomaly.py <repo>/app/tests/_work/normal/logs
   ```
2. 終了コード・`report.md`・標準出力を取得する。
3. リポジトリ側で実行した A001 の `report.md` と、実行日時・対象ディレクトリの行を除いて比較する。

**ケース B: `vendor/` 不一致での終了コード 7**

4. `{tmp}/dist/app/vendor/{platform_tag()}/` を作り、その中に `duckdb/__init__.py` を置いて `raise ImportError("broken vendor")` を書く。
   * **これだけでは終了コード 7 にならない。** `docs/P003-backend-spec.md` **DS-14-02** のとおり、実装は vendor の読み込みに失敗すると `sys.path` を元に戻してから**環境の DuckDB へフォールバックする**(意図された正しい挙動である)。
   * **したがって、起動時に `PYTHONPATH` へ壊れた `duckdb` パッケージを置き、環境側の DuckDB も隠すこと。**
5. さらに、`vendor/` に別のタグのディレクトリを 2 つ作る(`linux-x86_64-cp39`, `win-amd64-cp38` など)。メッセージの「用意されている vendor 一覧」を確認するため。
6. 同じコマンドで起動する。
7. 終了コードと標準エラーを取得する。

**ケース C: 不要ファイルの非依存**

8. `{tmp}/dist/app/tests/` を**丸ごと削除**する(テストは配布物に不要である)。
9. 同じコマンドで起動し、正常に動くことを確認する。

## 【実行コマンド】

```
python -m unittest discover -s app/tests/acceptance -t app -p "test_a010_portability.py" -v
```

* テストファイルは `app/tests/acceptance/test_a010_portability.py` として**新規作成する**。

## 【期待結果】

| # | ケース | 確認項目 | 期待値 |
| --- | --- | --- | --- |
| 1 | A | 終了コード | **0** |
| 2 | A | `report.md` | 生成される |
| 3 | A | **結果の一致** | A001 の結果と、実行日時・対象ディレクトリの行を除いて**完全一致** |
| 4 | A | **フォールバックの記録** | 標準出力の起動ログと `report.md` の 1 章に、**「vendor を使わず環境の DuckDB を使用しています」相当の記録**が出ている(`docs/P003-backend-spec.md` DS-14-02 の ★ACCEPTED★ が要求。**省略されていたら FAIL**) |
| 5 | A | インストール操作 | **`pip install` を一切行わずに動作している**(テストの中で `pip` を呼ばない) |
| 6 | B | 終了コード | **7** |
| 7 | B | メッセージ | 標準エラーに次の 4 つがすべて含まれる: **期待した vendor ディレクトリのパス** / **実行中の環境(OS・アーキテクチャ・Python 版)** / **用意されている vendor の一覧**(手順 5 で作った 2 つを含む) / **README への案内** |
| 8 | B | `report.md` | 生成されない |
| 9 | C | 終了コード | **0**(`tests/` が無くても動く = 配布物にテストが不要である) |
| 10 | C | 結果 | ケース A と同一 |

## 【合否判定基準】

* **PASS**: 10 項目すべてが期待値と一致する。
* **FAIL**: 1 項目でも一致しない。**特に項目 4(フォールバックの記録)が満たされない場合、閉域環境での `vendor/` 同梱漏れに気づけなくなるため FAIL とする。**
* **BLOCKED**: ビルド確認の失敗、または A001 が FAIL。
* **NOT RUN**: 実行環境の問題で実行自体ができなかった。

## 【失敗時に記録する内容】

* **失敗ログ抜粋**: 各ケースの終了コードと標準エラー全文。項目 3・10 の失敗では `difflib` による差分。項目 7 の失敗では、**実際に出たメッセージの全文と、欠けている要素**を明記する。
* **再現手順**: コピーの方法(除外したパス)、`vendor/` の作り方、起動コマンド。
* **影響がありそうなモジュール**:
  * 項目 1 の失敗 → `s_anomaly.py`(`sys.path` の解決が相対パス依存になっている)、または `app/` の外への依存
  * 項目 4 の失敗 → `bootstrap.py`(DS-14-02 項番 2 のログ出力漏れ)、または `reporter.py`(1 章への `vendor_note` の出力漏れ)
  * 項目 6・7 の失敗 → `bootstrap.py`(DS-14-03 のメッセージ書式)
  * 項目 9 の失敗 → `src/` が `tests/` に依存している(あってはならない)
* **テスト指示との矛盾の疑い**: ケース B の再現方法についての当初の懸念(環境の DuckDB を隠せず再現しない)は、P201 の実測で的中したため、【実行手順】手順 4 に `PYTHONPATH` で環境側も隠す手順を追記して解消済みである。**フォールバック自体は DS-14-02 が定めた正しい挙動であり、アプリの欠陥ではない。**

## 【修正禁止事項】

* **アプリケーションコードを修正しない。**
* **コピー先で `pip install` を実行しない。** それを行うと本テストの意味が失われる。
* **項目 4 のフォールバック記録を「軽微だから」として見逃さない。**
* **失敗したテストをスキップしない。**
* 失敗は記録し、**P202 以降へ引き渡す。**

## 【次タスクへ進む条件】

* 結果をテスト記録に残したこと。**`vendor/` からの実読み込みは本テストでは確認できない旨を、申し送りとして必ず記録すること。**
* `docs/P009-acceptance-direction.md` の A010 行を `[x]` に更新したこと。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、次のテストタスクに自動的に進んでください。

---

> **P012 修正記録**: 矛盾点 #2 にもとづき、【実行コマンド】を `python -m unittest app.tests.*` 形式から `discover -t app` 形式へ改めた。詳細は `docs/P011-impact-analysis.md` 2.2 を参照。

---

> **P202/F002 修正記録**: 矛盾点 #5 にもとづき、ケース B(終了コード 7)の再現手順に「`PYTHONPATH` で環境側の DuckDB も隠す」を追記した。壊れた vendor だけでフォールバックが働くのは DS-14-02 が定めた正しい挙動である。期待結果(終了コード 7 とメッセージの 4 要素)は変えていない。
> 詳細は `docs/P202-fix-plan/fixed/F002-test-direction-corrections.md` を参照。


---

# 【CR-001〜CR-004 による改訂】2026-09-05

**本テスト指示は、CR-001〜CR-004 の適用にともない次のとおり変更する。**
上記の本文のうち、下記と矛盾する記述は本節を正とする。

## 共通の変更点

**出力が単一の `report.md` から、次のファイル群に変わった**(P002 UI-04-F01)。

| ファイル名 | 内容 |
| --- | --- |
| `report_{HOST}_{yyyymm}.md` | ホスト × 年月ごとのイベント本文 |
| `report_summary_{yyyymm}.md` | 年月ごとの全ホスト横断サマリ |

* **本文中の「`report.md`」は、すべて「レポート群」に読み替える。**
* ファイルの存在確認は、**`report_*.md` のグロブ**で行う。
* 内容の確認は、**全ホスト別ファイルを合わせた集合**に対して行う。
* `app/tests/acceptance/_harness.py` の `report_path()` は **`report_paths()`(一覧を返す)に置き換える**。

## 固有の変更点

**本テストは `report.md` の存在・内容を確認しているため、レポート群に読み替える。**
確認する内容そのものは変わらない。
