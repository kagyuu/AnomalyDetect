# P010 設計横断レビュー(6 回目) — CR-011 対応

* 実施日: 2026-09-08
* 実施フェーズ: **P903 内部の P010**
* 対象: `docs/P001-requirement.md` / `P002` / `P003` / `P004` / `P006` / `P007` / `ADR.md` / `ArchitectureHandbook.md` / `README.md` / `INDEX.md` / `app/INDEX.md`
* 直前のレビュー: `docs/P010-design-review-5.md`(CR-010)

---

## 1. 判定

**合格。設計文書間の矛盾は無い。**

* 検出した不整合: **0 件**
* ★FIXME★ の新規追加: **2 件**(FR-142b の 3 列を落とす判断 / `nfsd` を 1 ホストだけ生成する判断)
* ★FIXME★ の解消: **0 件**

**CR-010 のときに 6 件の不整合が出たのに対し、今回 0 件だったのは
本CRが「対応表の拡張」に収まっているためである。**
新しいモジュール・テーブル・設定セクションのいずれも増えていない。

---

## 2. 数の突き合わせ(機械的に確認した項目)

**同じ集合が複数の文書とコードに書かれている。数えて一致を確認した。**

| 集合 | P001 | P002 | P003 | 実装 | 一致 |
| --- | --- | --- | --- | --- | --- |
| 活動種別 | FR-142 → **10** | 2.2 の既定値 → **10 語** | DS-SAR-02 の表 → **10** | `SAR_ACTIVITIES` = 10 / `WANTED` = 10 / `ACTIVITY_RULES` = 10 | ○ |
| sar のメトリクス | FR-142 の表 → **36** | UI-04-01a の和名表 → **36** | — | `sar.ALL_METRICS` = **36** | ○ |
| `DETECTABLE_METRICS` | — | — | DS-07-07b | **47**(既存 11 + sar 36) | ○ |
| ALG-C1 の分類 | FR-148 → self 8 / 除外 28 | — | DS-08-C1-01a / 02 | `SELF_RATIO_METRICS` 8 + `NO_CEILING["sar"]` 28 = **36** | ○ |
| メトリクスの和名 | — | UI-04-01a | — | `METRIC_LABELS` に**漏れなし** | ○ |

**36 = 8 + 28 が成り立っている。** どちらにも入らないメトリクスは無い。

**実装どうしの二重定義も確認した。**

| 対 | 確認 |
| --- | --- |
| `gen_testdata.SAR_WANTED_NFS/NFSD` ↔ `loaders.sar.WANTED` | **一致**(`test_gen_testdata_constants_match` が固定) |
| `sar.ALL_METRICS` ↔ `metrics.DETECTABLE_METRICS` | **一致**(`test_all_sar_metrics_are_detectable` が固定) |

---

## 3. 矛盾が無いことを確認した項目

| 観点 | 確認内容 | 結果 |
| --- | --- | --- |
| 判別列の一意性 | `retrans_s` は NFS にしか、`scall_s` は NFSD にしか現れない。**紛らわしい対(`read_s` / `sread_s`)を完全一致で分けている** | ○ |
| 取り込まない列 | FR-142b(`packet_s` / `udp_s` / `tcp_s`)と `WANTED["nfsd"]` が一致 | ○ |
| データモデル | **`sar` テーブルの定義を変更していない。** P001 5.3a / P002 6.2.3a / `schema.py` は無変更 | ○ |
| ALG-C1 | 「NFS は全て対象外」が P001 FR-148 / P003 DS-08-C1-02 / `NO_CEILING` で一致 | ○ |
| 既定値 | `[sar] activities` の既定が P001 13.3 / P002 2.2 / `config.SAR_ACTIVITIES` / `settings.properties` / README 4.6 の**5 箇所で一致** | ○ |
| テストデータ | 「`nfs` は 3 ホスト、`nfsd` は 1 ホスト」が P001 18.1 / P002 8.2 / `gen_testdata` で一致 | ○ |
| 後方互換 | NFS を含まない `sa-*.csv` でも動くこと。**列は「見出しに無ければ静かに飛ばす」既存規則(DS-SAR-03)がそのまま効く** | ○ |
| 図と本文 | **図の変更は無い。** モジュールも依存も増えていないため(P002 7.1 / P005 4章) | ○ |
| ADR | ADR-022(比率は一次の検知対象)と ADR-023(縦持ち)の**範囲内**である。新しい決定を含まない | ○ |

---

## 4. 新規の ★FIXME★(2 件)

| # | 箇所 | 内容 |
| --- | --- | --- |
| 1 | `docs/P001-requirement.md` FR-142b | **`packet_s` / `udp_s` / `tcp_s` を落とす判断は Agent のものである。** 実データでの確認が要る |
| 2 | `docs/P001-requirement.md` 18.1 | **`nfsd` をテストデータで 1 ホストだけに出す判断は Agent のものである。** 実環境では全ホストに(0 の値で)現れる |

**いずれも「取り込む範囲」に関する判断であり、実装の正しさには影響しない。**
`docs/.mode` が `一気通貫` であるため、納品(P302)まで残す。

---

## 5. 次のフェーズへの引き継ぎ

**不整合が 0 件のため、P011・P012 は実行しない。** Overview Step(P021・P022)へ進む。
