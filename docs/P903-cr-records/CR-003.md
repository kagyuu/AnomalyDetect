# CR-003 — イベント ID の体系を `EVT-YYYYMMDD-{HOST}-999` にする 対処記録

変更要求: [`docs/P901-cr-direction/CR-003.md`](../P901-cr-direction/CR-003.md)

**本 CR は CR-001〜CR-004 の 4 件と一体で適用する。** いずれも「レポートが 4K トークンの
ローカル LLM で扱えない」という同一の課題に対するものであり、単独では目的を達成しない
(`docs/CR.md` の補足を参照)。したがって P903 の内部再実行(P002〜P302)は **4 件まとめて 1 回**行う。

## スコープ決定(P903が着手前に記載)

### 影響カテゴリの分類

* [x] **UI表示文言のみ** — に近いが、ID は他ファイルからの参照キーでもあるため下記も該当する
* [ ] 画面項目追加・変更(バックエンド契約変更なし)
* [x] **API契約変更** — レポートに出力される識別子の形式が変わる
* [ ] データモデル変更
* [ ] 非機能・インフラ変更

### 対象とするフェーズ

| フェーズ | 対象とする理由 |
| --- | --- |
| **P002**(UI設計) | 出力ファイルの外部仕様 UI-04 が変わる。**本 CR 群の中心** |
| **P003**(詳細設計) | M11 correlate / M12 reporter / M09 events の設計が変わる |
| **P004**(トレーサビリティ) | FR-070〜FR-078 の対応先が変わる。**FR-078 が新設**されたため行の追加が要る |
| **P005**(実装計画) | 変更対象スプリント(U007 / U008)の特定に要る |
| **P006**(テスト計画) | レポートの検証方法が変わる(単一ファイル → ファイル群 + サマリ) |
| **P007**(実装指示) | U007(イベント・相関)と U008(レポート)の指示を書き換える |
| **P008**(結合テスト指示) | T005(S7→S8→S7' の順)・T006(レポートと正解の突き合わせ)・T007(一時ファイル)が影響を受ける |
| **P009**(受け入れテスト指示) | A001 / A003 / A004 / A005 / A009 / A010 / A011 がいずれも `report.md` を読んでいる |
| **P020**(ソースINDEX) | `reporter.py` / `correlate.py` の役割が変わる |
| **P021**(ADR) | **ADR-010 と ADR-014 に抵触する**(下記) |
| **P022**(ArchitectureHandbook) | 既知の制約 #13(report.md が大きすぎる)が解消に向かう |
| **P101〜P104**(Executor) | 実際にコードを変更するため |
| **P201**(実装横断レビュー) | 上記に実質的な変更が入るため |
| **P301 / P302**(Closing) | 目次・成果物一覧・README の整合を保つため |

**P011・P012** および **P202〜P205** は、**条件付きで対象になる**(P010 / P201 の結果次第)。

### 対象外としたフェーズ

| フェーズ | 再オープン不要と判断した理由 |
| --- | --- |
| **P000**(要求の原文) | **人間が書いた原文であり、Agent は改変しない。** 本 CR は P000 の記述と矛盾しない(P000 5章はイベントの書式のみを指定しており、ファイルの分割単位には言及していない) |
| **P001**(要件定義) | **P902 が更新済み。** P903 では触らない |
| **P010**(設計横断レビュー) | **対象外ではなく、P002〜P009 の更新後に実行する**(スコープ表の「対象」に含めるべきだが、Plan Loop の通常フローとして自動的に走るためここには挙げない) |

### 検知アルゴリズムに関する明示的な非対象

**10 個の検知アルゴリズム(`app/src/s_anomaly/detectors/`)は一切変更しない。**
本 CR 群はレポートの出力形式と、検知後のイベント同士の突き合わせ方を変えるものであり、
**何を異常と判定するかは変えない**。したがって検知件数・脅威度・イベントの集合は本 CR の前後で変わらない。
これは P904 の確認項目にもなる。

* **関連ADR**: 抵触するものは無い

### マイグレーション方式の確認

**「データモデル変更」に該当しないため対象外。**

本アプリは DuckDB を**インメモリで使い、1 回の解析ごとにテーブルを作り直す**。
永続化するデータストアを持たないため、マイグレーションという概念自体が無い
(`docs/P003-backend-spec.md`、`docs/P000-concept-analysis.md` 2章「一回の分析で読み込んだデータは捨ててよい」)。

なお「アプリケーションを停止・再起動しても正常に起動するか」は、**A004(再起動耐性)が確認済み**であり、
本 CR でもレポートのファイル名が変わるため **A004 の再実行が必要**である(スコープに含めた)。

## 対処内容(P903が完了時に記載)

### 変更したドキュメント

| 文書 | 変更内容 |
| --- | --- |
| `docs/P001-requirement.md` | **P902 が更新**(FR-070 / FR-071 / FR-072 / FR-074 / FR-075 / FR-076 / FR-077、**FR-078 を新設**、および NFR-007 / NFR-009 / NFR-010・終了コード表・モジュール表の整合)。CR 注記 19 箇所 |
| `docs/P002-frontend-spec.md` | 4 章を全面改訂。**UI-04-F01〜F10 と 4.5 節(同時アノマリーの生成規則。UI-04-C01〜C07)を新設**。4.1 を 4.1.1(サマリ)/ 4.1.2(ホスト別)に分割 |
| `docs/P003-backend-spec.md` | **10 章を `correlate` → `co_anomaly` に全面改訂**(DS-11-01〜08)。DS-09-07 改訂・**DS-09-08 新設**(ホスト抽出)、**DS-10-06 新設**(原因候補の読み替え表)、**DS-12-05〜08 新設**(分割・集計・4K の制約) |
| `docs/P004-traceability-matrix.md` | FR-070〜077 を更新、**FR-078 の行を追加**(OK 64 → 65 件) |
| `docs/P005-impl-plan.md` | U007 から `metrics` 依存が消えたこと、U008 の難易度を中 → 高へ |
| `docs/P006-test-plan.md` | 21 箇所。F-16 を全面改訂、T-05〜T-07 / A-01〜A-05 の観点を更新 |
| `docs/P007-impl-direction/U007-events.md` | **T6〜T9 を新設**、T1・T3・T4・T5 を `[~]` へ |
| `docs/P007-impl-direction/U008-report.md` | **T5〜T8 を新設**、T1・T2・T4 を `[~]` へ |
| `docs/P008-test-direction/T005〜T007` | CR による改訂節を追記 |
| `docs/P009-acceptance-direction/A001〜A011`(A008 を除く 10 件) | CR による改訂節を追記 |
| `docs/P010-design-review-3.md` | **新設**(設計横断レビュー 3 回目) |
| `docs/ADR.md` | **ADR-010 を改訂**(前提が変わったが決定は有効)、**ADR-014 を撤回**(対象消滅)、**ADR-015 を新設**(レポートの 2 層構造) |
| `docs/ArchitectureHandbook.md` | 既知の制約 #13 / #15 を更新 |
| `app/INDEX.md` | `co_anomaly.py` ほかの記載を更新 |

### 変更したソースコード

**プロダクションコード(5 ファイル)**

| ファイル | 変更内容 |
| --- | --- |
| `app/src/s_anomaly/co_anomaly.py` | **新設。** 掃引法で区間の重なりを求める。**2 系統の出力**を持つ(下記) |
| `app/src/s_anomaly/correlate.py` | **削除。** `metrics` の範囲結合による相関算出を廃止 |
| `app/src/s_anomaly/events.py` | `extract_host` を移設(DS-09-08)、ID 採番を「日 × ホスト」へ、`Event.co_anomalies` を追加 |
| `app/src/s_anomaly/causes.py` | 判定の入力を `concurrent_by_metric` へ。`_flat` を「持続的な上昇が無いこと」に精密化 |
| `app/src/s_anomaly/reporter.py` | `write_reports` / `render_summary` / `render_host_digest` / `safe_host` を新設。`render_report` を削除 |
| `app/src/s_anomaly/cli.py` | S9 を `write_reports` へ、S8 を `co_anomaly.collect` へ |

**テストコード(17 ファイル)**

`test_co_anomaly.py`(新設・22 件)のほか、`_harness.py` / `report_parser.py` を複数ファイル対応にし、
レポート書式に依存する 15 のテストファイルを新仕様へ追随させた。

### 事前のスコープ想定と実際の差異

**スコープの範囲そのものに差異は無かった。** 着手前に「対象」とした P002〜P009・P020〜P022・
P101〜P104・P201〜P205・P301・P302 はすべて実際に対象になり、「対象外」とした P000・P001 には
触れていない(P001 は P902 が更新済み)。

**ただし、設計の内容に 3 件の想定外があった。いずれも実測で発見し、その場で是正した。**

#### 差異 1: 原因候補ルールが構造的に発火しなくなった(設計の欠陥)

* **想定**: DS-10-06 の読み替えにより「条件が緩くなる」(CR-01 / CR-04 が成立しやすくなる)。
* **実際**: **まったく発火しなくなった。**
* **原因**: CR-01(メモリリーク)は「`ou` が floor_rise = **観点2**」を主体とするルールだが、
  観点2 のイベントには `co_anomalies` を設定しない設計にしたため、条件を評価できなかった。
  CR-02 / CR-04 / CR-06 も同じ理由で発火不能であった。
* **是正**: `co_anomaly` の出力を **2 系統**に分けた。
  * `co_anomalies` — **レポートに出す**もの。観点1 のみ(依頼者の指示どおり)
  * `concurrent_by_metric` — **原因候補ルール専用**。同一ホストで区間が重なるイベントを
    メトリクス別に 1 件。**観点を問わず、レポートには出さない**
* **判断の根拠**: 依頼者の指示は**レポートの表示**についてのものであり
  (「観点2 のみのイベントでは同時のアノマリーを出さなくていい」)、原因候補の内部推論を
  観点1 に限る趣旨とは読めない。メモリリークの原因候補が失われるのは P000 の主目的
  (観点2 = メモリリークの検出)に反する。

#### 差異 2: 「変化なし」の読み替えが厳しすぎた

* **実際**: 是正 1 のあとも CR-01 が発火しなかった。14 日間の `ou` イベントには
  期間中のあらゆるイベントが区間として重なるため、`eu` の `level_shift` が
  「eu に変化あり」と判定されていた。
* **是正**: 否定条件(「変化なし」「横ばい」)の判定を
  **持続的な上昇(`trend_up` / `floor_rise`)が無いこと**に限定した。
  単発のスパイクや水準シフトは阻害要因にしない。
* **根拠**: CR-01 の「eu に変化なし」は「若い世代も一緒に増え続けてはいない = 単なる負荷増ではない」
  という意図であり、eu の単発スパイクはこの判断を覆さない。

#### 差異 3: 破損データの読み飛ばし件数が失われた(FR-014 違反)

* **実際**: 検知 0 件だとホスト別ファイルが作られず、「実行時の注意事項」の記録先が消えていた。
* **是正**: サマリに「5. 実行時の注意事項」を追加した(P002 UI-04-F10 が定めていた
  「ホストに紐づけられないものはサマリ側に載せる」の実装漏れであった)。

#### 差異 4: 4K の上限設定が甘かった

* **想定**: サマリは 2,764 バイト(30 日規模の試算)で 4K に収まる。
* **実際**: **90 日規模で 7,254 バイト**になった。「4. 複数ホストで同時に発生したアノマリー」で
  16 ホスト分の名前を 1 行に列挙していたことが主因(3,683 バイト)。
* **是正**: **すべての表に行数上限を置き、規模が伸びても増えない構造にした**
  (`MAX_HOST_ROWS`=10 / `MAX_BUCKET_ROWS`=10 / `MAX_CROSS_HOST_ROWS`=8 /
  `MAX_LISTED_HOSTS`=3 / `MAX_LISTED_METRICS`=3)。DS-12-08 を実測値で更新した。

### 実行したテストと結果

`docs/test-records/20260905-0530-test-record.md` に記録した。要旨は次のとおり。

| 対象 | 1 回目 | 2 回目 |
| --- | --- | --- |
| ビルド `python -m compileall -q app` | 終了コード 0 | — |
| 単体テスト | `Ran 410 tests in 201.010s` / `OK (skipped=1)` | `Ran 410 tests in 213.203s` / `OK (skipped=1)` |
| 結合テスト | `Ran 108 tests in 261.665s` / `OK` | `Ran 108 tests in 274.864s` / `OK` |
| 受け入れテスト | `Ran 125 tests in 271.343s` / `OK (skipped=15)` | `Ran 125 tests in 273.744s` / `OK (skipped=15)` |

**件数が増えているのは、CR による確認項目を追加したためである**(単体 376 → 410、
結合 105 → 108、受け入れ 122 → 125)。

**A006(確定規模での性能)**

| ケース | CR 適用前 | **CR 適用後** |
| --- | --- | --- |
| 通常 30 日(950,400 行) | 147.4s | **70.8s(-52%)** |
| 大規模 90 日(2,851,200 行) | 531.3s | **220.4s(-59%)** |

**4K の達成状況(90 日規模の実測)**

| 出力 | サイズ | 4K |
| --- | --- | --- |
| `report_summary_202606.md` | **3,830 バイト** | ○ |
| `report_summary_202607.md` | 3,813 バイト | ○ |
| `report_summary_202608.md` | 3,793 バイト | ○ |
| ホスト別ファイルの「1. このホストの集計」(最大) | **674 バイト** | ○ |


## 反映確認(P904が記載)

### 「期待する振る舞い」の確認結果

| # | 期待する振る舞い | 確認方法 | 結果 |
| --- | --- | --- | --- |
| 1 | ID が `EVT-{開始日 YYYYMMDD}-{ホスト名}-{連番 3 桁}` になる | 90 日規模の全 21,307 件を正規表現で検証。**形式不一致 0 件** | **OK** |
| 2 | **連番は「日 × ホスト」ごとに 1 から振る** | **単体** `test_events.py::TestEventIdIncludesHost::test_sequence_per_host_per_day`(同じ日の別ホストがそれぞれ 001 から) | **OK** |
| 3 | 同一入力に対して常に同一の ID | **単体** `test_events.py::TestEventIdIncludesHost::test_reproducible`(順序を変えても同じ ID)/ **A003** 再現性 | **OK** |
| 4 | ホスト名が特定できない系列でも失敗しない | **単体** `test_events.py::TestExtractHost::test_unknown_formats`(未知の書式・空文字・`None` で `"unknown"` を返す) | **OK** |
| 5 | 相手の ID から**どのホストのものか**が読み取れる | 実データの「同時に発生したアノマリー」欄の全明細行に `EVT-{日付}-{ホスト}-{連番}` 形式の ID が入っていることを確認(書式不一致 0 件) | **OK** |
| 6 | ID が 1 実行の中で重複しない | 90 日規模の全 21,307 件で **ID 重複 0 件** | **OK** |

### スコープ決定の妥当性

**妥当であった。** 想定どおり `events.py` / `reporter.py` / テストの更新で完結した。
**`extract_host` を `correlate.py` から `events.py` へ移した**のは、CR-002 で `correlate.py` を
廃止するため、採番が廃止予定のモジュールに依存しないようにする判断である(DS-09-08)。

### 反映確認の結論

**CR-003 の変更要求はすべて反映された。** 状態を `完了` とする。

★申し送り★ ID が長くなった(例: `EVT-20260601-host01-001` は 29 文字)。
**ホスト名の長さに上限を設けるかは人間の確認が必要**(P001 FR-072 の ★FIXME★)。


## ADR更新判定(P905が記載)

**ADR 更新: 不要**(理由: 識別子の表記規則の変更であり、技術的決定を含まないため)。

* イベント ID にホスト名を含める判断は、**CR-001 でレポートをホスト別に分割したことの帰結**である。
  分割したファイル間を相互参照するために必要になったものであり、
  独立した技術的決定ではない(`docs/P003-backend-spec.md` DS-09-07 / DS-09-08 に記載済み)。
* `extract_host` を `correlate.py` から `events.py` へ移した判断も、
  **CR-002 で `correlate.py` を廃止するための依存関係の整理**であり、ADR の粒度ではない。
* 既存の ADR を覆すものは無い。

