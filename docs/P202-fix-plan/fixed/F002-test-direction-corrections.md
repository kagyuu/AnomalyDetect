あなたはReviewer Loop(修正担当)です。以下の1修正タスクを実施してください。

# 【修正タスクID】F002 — テスト指示と上位文書の食い違い 6 件

## 【対応する失敗テスト】A001, A002, A004, A007, A010, A011

* 対応するテスト記録: `docs/test-records/20260903-0010-test-record.md` の各テストの「テスト指示との矛盾の疑い」欄
* 判定の出典: `docs/P201-review-report.md` 5章

**単一根本原因によるまとめではない点に注意。** 本ファイルは **6 件の食い違いを 3 系統に整理してまとめた**ものである。まとめてよい根拠は次のとおり。

* 6 件はいずれも**同一の結果区分「テスト指示側の誤り」**に属し、**いずれもソースコードを変更しない**。
* うち #2〜#4 は **UI-02-L01 という単一の根本原因**(設定ファイルの読み込み位置に関する指示の誤解)から生じている。
* #5 は **DS-14-02 という単一の根本原因**(vendor フォールバック仕様)から 2 箇所に現れている。
* #1 と #6 はそれぞれ独立だが、いずれも文書 1 行の訂正であり、個別ファイルに分ける実益がない。

**いずれも A001〜A011 の FAIL を引き起こしていない。** P201 の実行時に、テスト側で上位文書に従った実装を採ることで回避した。本タスクは**指示文書を実装・上位文書に追随させる**ものである。

## 【障害記録】

* **原因区分: テスト指示側の誤り**(`SKILL-P202-fix-plan.md` の一級の結果区分)
* **アプリケーションコードの欠陥ではない。** 6 件すべてについて、実装は上位文書のとおりに動いている。

### #1 — A001 の誤検知基準が古い

| 項目 | 内容 |
| --- | --- |
| 該当 | `docs/P009-acceptance-direction/A001-normal-run.md`【期待結果】項目 10 |
| 現在の記述 | 「**誤検知**: `clean_series` に属するイベントが **0 件**」 |
| 矛盾する上位文書 | `docs/P002-frontend-spec.md` **UI-08-03**: 「`clean_series` に属する検知点が、その系列の総点数の **1% 未満**であり、かつ **SEVERE のイベントが 0 件**であること」 |
| 経緯 | UI-08-03 は **ADR-012** にもとづき P102 の実装段階で改められた。A001 の期待結果表がこれに追随していない |
| なぜ旧文言が誤りか | **統計的手法である以上「0 件」は達成不能**である。4,000 点の系列に 2σ の判定を当てると、正規分布のノイズだけで約 180 点が偶然閾値を超える(UI-08-03 の ★FIXME★ に明記) |
| A001 自身の矛盾 | A001【実行手順】5 は「`expected.json` と突き合わせる(**`docs/P002-frontend-spec.md` UI-08-03 の 3 判定**)」と、UI-08-03 を参照するよう指示している。つまり A001 の中でも手順と期待結果表が食い違っている |

**カバレッジが失われないことの根拠**: 訂正後も「`clean_series` に SEVERE が 0 件」の確認は残る(`test_a001_normal_run.py` の `test_10_no_severe_on_clean_series`)。加えて、結合テスト T004(`test_detectors_run.py`)が Detection レベルで検知率を測っており、レポート文字列レベルでは T006(`test_report_expected.py`)が同じ観点を確認している。**同じ観点を確認するテストが 3 段階に存在する。**

### #2〜#4 — `settings.properties` の置き場所

| 項目 | 内容 |
| --- | --- |
| 該当 | A002【使用するテストデータ】の終了コード 3 / 6 の行、A004【事前準備】3、A007【事前準備】3 |
| 現在の記述 | いずれも「**作業ディレクトリに** `settings.properties` を置く」と読める |
| 矛盾する上位文書 | `docs/P002-frontend-spec.md` **UI-02-L01**: 「**`s_anomaly.py` と同じディレクトリ**の `settings.properties` を読む。存在しない場合は全項目を既定値として動作し、その旨を進捗ログの INFO に出す(P001 FR-081)」 |
| 実装 | `app/src/s_anomaly/cli.py` は `config.load(os.path.join(root, "settings.properties"), progress)` を呼んでおり、`root` は `app_root()`(= `s_anomaly.py` の位置)である。**UI-02-L01 のとおりである** |
| 回避方法(P201 で採用) | ケースごとに `app/` を一時ディレクトリへ複製し、**複製側の `settings.properties`** を差し替えて起動した(`app/tests/acceptance/_harness.py` の `make_dist`) |

**カバレッジが失われないことの根拠**: 訂正は「どこに設定ファイルを置くか」の手順のみであり、確認する内容(終了コード 3 / 6、メモリ退避)は変わらない。設定ファイルの読み込み位置そのものは、単体テスト `test_config.py` と `test_cli_args.py` が別途確認している。

**A004 について補足**: A004【事前準備】3 の意図は「残骸の確認対象を作業ディレクトリ内に閉じる」ことである。**`temp_directory` の既定値がそもそも `./tmp`(作業ディレクトリ相対)であるため、設定ファイルを置かなくても意図は満たされる。** 訂正時はこの点も書き添えること。

### #5 — vendor 不一致で終了コード 7 を再現する方法

| 項目 | 内容 |
| --- | --- |
| 該当 | A002【事前準備】5、`docs/P009-acceptance-direction/A010-portability.md`【実行手順】ケース B(手順 4〜6) |
| 現在の記述 | 「`app/vendor/{platform_tag()}/` に `duckdb/__init__.py`(`raise ImportError`)を置く。**これにより vendor 経由の import が失敗し、かつ `sys.path` の先頭に入るため環境の DuckDB も隠れる**」 |
| 実測 | **隠れない。終了コード 0 で正常終了する。** |
| 矛盾する上位文書 | `docs/P003-backend-spec.md` **DS-14-02**: vendor からの読み込みに失敗した場合、`sys.path` を元に戻してから**環境の DuckDB へフォールバックする**。実装(`bootstrap.load_duckdb`)もそのとおりである。**これは意図された正しい挙動である** |
| 予告 | A002【事前準備】5 の ★FIXME★ が「実装の `sys.path` 挿入位置によっては環境の DuckDB が優先され再現しない可能性がある」と予告しており、A010 の【テスト指示との矛盾の疑い】欄も同じ懸念を挙げていた。**その懸念が的中した** |
| 回避方法(P201 で採用) | 壊れた vendor に加えて、`PYTHONPATH` に壊れた `duckdb` パッケージを置いて**環境側の DuckDB も隠した**うえで起動した |

**カバレッジが失われないことの根拠**: 訂正は再現手順のみであり、確認する内容(終了コード 7 と、メッセージに含まれる 4 要素)は変わらない。**加えて、P201 では「壊れた vendor だけでは終了コード 0 のまま(フォールバックが働く)」ことを確認するテストを新設した**(`test_a002_exit_codes.py` の `test_11b_vendor_fallback_alone_does_not_fail`)。**カバレッジはむしろ増えている。**

### #6 — Windows でのディレクトリ読み取り専用化

| 項目 | 内容 |
| --- | --- |
| 該当 | `docs/P009-acceptance-direction/A011-user-privilege.md`【事前準備】4 |
| 現在の記述 | 「Windows では `attrib +R`、Linux では `chmod -w` を適用する」 |
| 実測 | **Windows のディレクトリに対する読み取り専用属性は、配下のファイル作成を防がない** |
| 矛盾 | A011 自身の ★FIXME★ が既にこの点を指摘し、「設定できない場合は、代わりに**実行前後のファイル一覧を比較**する方法で確認する」と代替手段を併記している |
| 回避方法(P201 で採用) | **指示が併記する代替手段(実行前後のファイル一覧の比較)で確認した。** 入力ファイル自体には読み取り専用属性を付与している |

**カバレッジが失われないことの根拠**: 確認したい内容は「アプリが入力ディレクトリへ書き込まないこと」である。ファイル一覧(パス・サイズ・更新日時)の実行前後比較は、これを**より直接的に**確認している。訂正は ★FIXME★ を「代替手段で確認済み」として解消するものである。

## 【参照ファイル】

| ファイル | 参照理由 |
| --- | --- |
| `docs/P009-acceptance-direction/A001-normal-run.md` | #1 の修正対象 |
| `docs/P009-acceptance-direction/A002-exit-codes.md` | #2 と #5 の修正対象 |
| `docs/P009-acceptance-direction/A004-restart-resilience.md` | #3 の修正対象 |
| `docs/P009-acceptance-direction/A007-memory-spill.md` | #4 の修正対象 |
| `docs/P009-acceptance-direction/A010-portability.md` | #5 の修正対象 |
| `docs/P009-acceptance-direction/A011-user-privilege.md` | #6 の修正対象 |
| `docs/P002-frontend-spec.md` UI-08-03 / UI-02-L01 | #1 / #2〜#4 の根拠 |
| `docs/P003-backend-spec.md` DS-14-02 | #5 の根拠 |
| `docs/ADR.md` ADR-012 | #1 の経緯 |
| `app/tests/acceptance/*.py` | **既に上位文書に従って実装済み。** 訂正後の指示と一致することを確認する |

## 【調査方針】

1. 上表の「矛盾する上位文書」を**実際に開いて該当箇所を確認する**。本ファイルの引用を鵜呑みにしない。
2. 各テストコードが、訂正後の指示と一致していることを確認する(**P201 で既に上位文書に従って実装済みであるため、コードの変更は不要なはずである**)。
3. 訂正によって確認カバレッジが失われないことを、上記「カバレッジが失われないことの根拠」に照らして確認する。

## 【修正方針】

**原因区分は「テスト指示側の誤り」である。ソースコードは変更しない。**

各指示書の該当箇所を、上位文書に合わせて訂正する。訂正にあたっては次を守る。

* **なぜ変えたのかを、根拠となる上位文書の名前とセクション番号とともに書き添える。** 単に文言を書き換えるだけにしない。
* 各ファイル末尾の「P012 修正記録」欄と同じ形式で、**「P202/F002 修正記録」**を追記する。
* #1 と #6 については、対応する ★FIXME★ を解消する(#6 は A011 の ★FIXME★ を「代替手段で確認済み」として書き換える)。
* **A002 と A010 の ★FIXME★(#5)は、予告が的中したことを明記して解消する。** 「実装の挙動が正しく、指示の想定が誤っていた」ことがわかる書き方にする。

**期待値の訂正は改ざんに当たらない**(`SKILL-P202-fix-plan.md` の「結果区分『テスト指示側の誤り』」)。ただし上記の根拠の明記が条件である。

## 【試行錯誤してよい範囲】

* `docs/` 配下の文書を読む
* テストコードを読む
* テストを再実行して、訂正後の指示と実際の挙動が一致することを確認する

**範囲外**: **アプリケーションコードの変更**(本タスクはコードを一切変更しない)。`app/tests/_work/normal/expected.json` の変更。上位文書(`docs/P001-requirement.md`・`docs/P002-frontend-spec.md`・`docs/P003-backend-spec.md`)の変更 — **これらが正であり、追随するのは指示書側である。**

## 【修正成功時に更新するdocs】

| 文書 | 更新内容 |
| --- | --- |
| `docs/P009-acceptance-direction/A001-normal-run.md` | 項目 10 を UI-08-03 に合わせる |
| `docs/P009-acceptance-direction/A002-exit-codes.md` | 設定ファイルの置き場所、終了コード 7 の再現方法 |
| `docs/P009-acceptance-direction/A004-restart-resilience.md` | 設定ファイルの置き場所(既定値で足りる旨も書き添える) |
| `docs/P009-acceptance-direction/A007-memory-spill.md` | 設定ファイルの置き場所 |
| `docs/P009-acceptance-direction/A010-portability.md` | ケース B の再現方法 |
| `docs/P009-acceptance-direction/A011-user-privilege.md` | 事前準備 4 の ★FIXME★ を解消 |
| `docs/P202-fix-plan/fixed/F002-test-direction-corrections.md` | 本ファイルを移し、訂正内容の詳細を追記する |
| `docs/P202-fix-plan/P202-fix-resolved.md` | 「変更したソースコード」欄に**「なし(テスト指示側の誤りのため、`docs/P009-acceptance-direction/*.md` を修正)」**と明記する |
| `docs/P202-fix-plan.md` | F002 の行を `[x]` にし、リンク先を `fixed/` へ書き換える |

## 【ロールバック条件】

**該当なし(ソースコード変更を伴わない)。**

ただし、訂正後の指示と既存のテストコードが食い違った場合は、**訂正の内容を見直す**こと(テストコードを指示に合わせて壊さない)。

## 【検証コマンド】

```
# コードを変更していないことの確認 (退避物との差分)
# 差分が app/ 配下に出ないこと

# 受け入れテスト (A006 を除く 109 件)。訂正後の指示と実際の挙動が一致することの確認
python -m unittest discover -s app/tests/acceptance -t app
```

## 【完了条件】

1. 6 件すべてについて、該当する指示書が上位文書に合わせて訂正されている。
2. 各訂正に、**根拠となる上位文書の名前とセクション番号**が書き添えられている。
3. 対応する ★FIXME★(#1 の A001、#5 の A002・A010、#6 の A011)が解消されている。
4. **`app/` 配下のファイルを 1 つも変更していない。**
5. 受け入れテスト 109 件が引き続き全件成功する。
6. 上記【修正成功時に更新するdocs】の更新が済んでいる。

## 重要:

* 作業開始前に現在の変更状態を確認してください。
* **本タスクではソースコードを変更しません。**
* 修正に成功した場合は、関連する docs/* も必要に応じて更新してください。
* 訂正の根拠(矛盾する上位文書の該当箇所、カバレッジが失われないこと)を必ず記録してください。**根拠なしに期待値を書き換えることは改ざんです。**

---

# 【P203 実施記録】

* 実施日: 2026-09-04
* 実施フェーズ: P203(修正実施)
* 結果: **RESOLVED**

## 実施内容

**アプリケーションコードは 1 行も変更していない**(完了条件 4)。訂正はすべて
`docs/P009-acceptance-direction/*.md` に対して行った。

| # | 訂正した指示書 | 訂正内容 | 根拠とした上位文書 |
| --- | --- | --- | --- |
| 1 | `A001-normal-run.md`【期待結果】項目 10 | 誤検知の基準を「イベント 0 件」から「`clean_series` の検知点が総点数の 1% 未満、かつ SEVERE が 0 件」へ改めた | `docs/P002-frontend-spec.md` **UI-08-03**(`docs/ADR.md` **ADR-012**) |
| 2 | `A002-exit-codes.md`【使用するテストデータ】終了コード 3 / 6 | `settings.properties` の置き場所を「作業ディレクトリ」から「`s_anomaly.py` と同じディレクトリ」へ改めた | `docs/P002-frontend-spec.md` **UI-02-L01**(`docs/P001-requirement.md` FR-081) |
| 3 | `A004-restart-resilience.md`【事前準備】3 | 同上。あわせて `temp_directory` の既定値が `./tmp`(作業ディレクトリ相対)であるため、**設定ファイルを置かなくても本テストの意図は満たされる**旨を書き添えた | 同 **UI-02-L01** |
| 4 | `A007-memory-spill.md`【事前準備】3 | 同上 | 同 **UI-02-L01** |
| 5 | `A002-exit-codes.md`【事前準備】5 / `A010-portability.md`【実行手順】ケース B | 終了コード 7 の再現手順に「`PYTHONPATH` で環境側の DuckDB も隠す」を追記した。壊れた vendor だけでは**フォールバックが働いて終了コード 0 になるのが正しい挙動**である | `docs/P003-backend-spec.md` **DS-14-02** |
| 6 | `A011-user-privilege.md`【事前準備】4 | Windows のディレクトリ読み取り専用属性では確認できないため、指示が併記していた代替手段(実行前後のファイル一覧の比較)を主たる手段として確定した | A011 自身が併記していた代替手段 |

## ★FIXME★ の解消(完了条件 3)

| 指示書 | 解消した ★FIXME★ | 解消のしかた |
| --- | --- | --- |
| `A001-normal-run.md` | 誤検知 0 件の達成可能性 | UI-08-03 に合わせて訂正し、**残存 ★FIXME★ 0 件** |
| `A002-exit-codes.md`【事前準備】5 | 「環境の DuckDB が優先され再現しない可能性」 | **予告が的中した**ため ★ACCEPTED★ へ転換 |
| `A002-exit-codes.md`【事前準備】6 | 終了コード 99 の再現方法(F002 の対象 6 件外) | P201 の実測で再現できていたため ★ACCEPTED★ へ転換 |
| `A010-portability.md` ケース B | 同 #5 | ★ACCEPTED★ へ転換し、**残存 ★FIXME★ 0 件** |
| `A011-user-privilege.md`【事前準備】4 | Windows のディレクトリ読み取り専用化 | 代替手段を確定し ★ACCEPTED★ へ転換 |

現在、6 つの指示書に残る `★FIXME★` の文字列は、**いずれも「かつて ★FIXME★ を
付していたが解消した」ことを説明する地の文**であり、未解決の目印としては
残っていない。

## 検証(完了条件 5)

`docs/P202-fix-plan.md` 4.4 および CLAUDE.md の「テストスイートは続けて 2 回実行する」
規則に従い、**連続 2 回**実行した。

```
python -m unittest discover -s app/tests/acceptance -t app
```

| 実行 | 結果 |
| --- | --- |
| 1 回目 | `Ran 122 tests in 501.553s` / `OK (skipped=13)` |
| 2 回目 | `Ran 122 tests in 510.945s` / `OK (skipped=13)` |

skipped=13 は A006 の 2 クラス(大規模 9 件 + 通常 4 件)であり、`S_ANOMALY_RUN_A006=1`
が無いために既定でスキップされる。**したがって実行されたのは 109 件**で、
`docs/P202-fix-plan.md` 4.4 の「受け入れ結合テスト(A006 を除く 109 件)」と一致する。
0 件で静かに成功してはいない。

## 訂正がカバレッジを失わせていないことの確認

| # | 失われないことの確認 |
| --- | --- |
| 1 | `test_a001_normal_run.py::test_10_no_severe_on_clean_series` が SEVERE 0 件を、結合テスト T004 が Detection レベルの検知率を、T006 がレポート文字列レベルを確認する。**3 段階に残っている** |
| 2〜4 | 訂正したのは設定ファイルの置き場所という手順のみ。確認内容(終了コード 3 / 6、メモリ退避)は不変。読み込み位置自体は単体テスト `test_config.py` / `test_cli_args.py` が確認する |
| 5 | 訂正したのは再現手順のみ。確認内容(終了コード 7 とメッセージの 4 要素)は不変。**加えて `test_a002_exit_codes.py::test_11b_vendor_fallback_alone_does_not_fail` が新設されており、カバレッジはむしろ増えている** |
| 6 | ファイル一覧(パス・サイズ・更新日時)の実行前後比較は、「入力ディレクトリへ書き込まないこと」を**属性による確認より直接的に**検証している |

## 変更したソースコード

**なし。** 原因区分が「テスト指示側の誤り」であり、実装はすべて上位文書のとおりに
動いていたため(`SKILL-P202-fix-plan.md` の結果区分「テスト指示側の誤り」)。
