# P010 設計横断レビュー(5 回目) — CR-010 対応

* 実施日: 2026-09-08
* 実施フェーズ: **P903 内部の P010**
* 対象: `docs/P001-requirement.md` / `P002` / `P003` / `P004` / `P005` / `P006` / `P007`〜`P009` / `ADR.md` / `CR.md`
* 直前のレビュー: `docs/P010-design-review-4.md`(CR-005〜CR-007)

---

## 1. 判定

**条件付き合格。** 検出した不整合 **6 件**のうち、**4 件は CR-010 が持ち込んだもの**であり、
**2 件は CR-009 までの反映漏れ**である。**全件を P012 で是正する。**

* 是正後に P010 を再実行し、残存 0 件を確認する。

---

## 2. メトリクス定義の突き合わせ(機械的に確認した項目)

**同じ集合が 5 つの文書に書かれている。数を数えて一致を確認した。**

| 集合 | P001 | P002 | P003 | P007(U010) | 一致 |
| --- | --- | --- | --- | --- | --- |
| 取り込むメトリクス(重複除く) | FR-142 の表 → **22** | UI-04-01a の和名表 → **22** | DS-SAR-02/03 経由 | §4.5 `WANTED` → **22** | ○ |
| `SELF_RATIO_METRICS`(上限 100) | FR-148 の表 → **8** | - | DS-08-C1-01a → **8** | §5.3 → **8** | ○ |
| ALG-C1 対象外(sar) | FR-148 の表 → **14** | - | DS-08-C1-02 → **14** | §5.3 → **14** | ○ |
| 合計 | 8 + 14 = **22** | | | | ○ |
| 活動種別 | FR-142 → **8** | 2.2 の既定値 → **8 語** | DS-SAR-02 → **8** | §4.2 → **8** | ○ |

**22 = 8 + 14 が成り立っている。** すべてのメトリクスが「上限 100 で判定する」か
「ALG-C1 の対象外」のどちらかに分類されており、**どちらにも入らないメトリクスは無い。**

---

## 3. 検出した不整合

### 3.1 CR-010 が持ち込んだもの(4 件)

| # | 箇所 | 不整合の内容 | 是正方針 |
| --- | --- | --- | --- |
| 1 | `docs/P002-frontend-spec.md` 2.2 の `[sar] activities` 行 | **「違反時」列が `終了コード 3` だけになっている。** 本文(同じセル)と `docs/P003-backend-spec.md` DS-02-05 は「**未知の語は警告して無視**」と定めており、食い違って読める | 「違反時」列を「警告(未知の語)/ 終了コード 3(読み取り不能)」に改める |
| 2 | `docs/P007-impl-direction/U010-sar.md` §4.5 | **`await` は Python の予約語である**(3.7 以降)。文字列としてしか使わないため実害は無いが、**実装者が属性名・変数名に使うと構文エラーになる**。注意書きが無い | §4.5 に注意書きを足す |
| 3 | `docs/P006-test-plan.md` 5.3 の受け入れ一覧 | **A-12 / A-13 の行が無いまま A-14 を足した。** A012(タイムゾーン)・A013(チャート)は `docs/P009-acceptance-direction/` に実在する。**表だけが CR-008・CR-009 の時に更新されていない**(既存の漏れだが、A-14 を足したことで表の連番が飛んで見える) | A-12 / A-13 の行を補って連番を通す |
| 4 | `docs/P005-impl-plan.md` 3.1 | **U009 の列が無い**(CR-005〜007 時の反映漏れ)。CR-010 で U010 の列を足したため、**U008 の次が U010 になり、U009 が抜けているように見える** | 表の直下に脚注で明記する(表の再構成はしない) |

### 3.2 CR-009 までの反映漏れ(2 件)

**CR-010 とは無関係だが、CR-010 の文書と正面から矛盾するため是正する。**

| # | 箇所 | 不整合の内容 | 是正方針 |
| --- | --- | --- | --- |
| 5 | `docs/P001-requirement.md` 17章「やらないこと・対象外」 | **「グラフ・図の生成。レポートはテキストのみ」と書かれたままである。** CR-009 で SVG の折れ線チャートを実装しており(FR-130〜136、13.2 節)、**同じ文書の中で矛盾している** | 当該行を削除し、CR-009 で対象になった旨を注記する |
| 6 | `docs/P003-backend-spec.md` 1.1 ディレクトリ構成 | **`charts.py`(M15)がツリーに無い。** CR-009 で新設したモジュールが設計文書のディレクトリ構成に反映されていない | 1 行追加する |

---

## 4. 矛盾が無いことを確認した項目

| 観点 | 確認内容 | 結果 |
| --- | --- | --- |
| 入力ファイル名 | `sa-{ホスト名}-{yyyymmdd}.csv` が P001 4章 / 4.3a / P002 8.2 / P003 DS-03-01 / README 付録A で一致 | ○ |
| `series_id` 書式 | `sar/{host}/{activity}/{device}` が P001 FR-144 / P002 6.3 / P003 DS-07-07a / U010 §5.4 で一致 | ○ |
| 論理 PK | 5 列であることが P001 5.3a / P002 6.2.3a / P003 DS-LD-06a / U010 §5.1 で一致 | ○ |
| タイムゾーン | 「設定値を正とし、表記では自動変換しない」が P001 FR-147 / P003 DS-SAR-05 / CR-010 本文で一致 | ○ |
| ホスト名 | 「アプリ側で正規化しない」が P001 FR-145 / 17章 / P003 DS-SAR-06 / P302 10.5 で一致 | ○ |
| ADR との関係 | ADR-005(`*_pct` は検知対象外)と FR-143(接頭 `pct_` は検知対象)が**書き分けられており矛盾しない**。P003 DS-07-07b が両者の関係を明示している | ○ |
| ADR-016 | 「sar では推定上限を使わない」が P001 FR-148 / P003 DS-08-C1-01a で一致。ADR-016 自体は他の系列で有効なまま | ○ |
| 図と本文 | P002 7.1 のシーケンス図に `M16` を反映済み。P005 4章の依存図に U010 を反映済み | ○ |
| 後方互換 | 「`sa-*.csv` が無ければ従来どおり」が CR-010 の期待する振る舞い / P006 F-22 / P008 T009 #15 / P009 A014 #13 で一致 | ○ |
| **イベント ID の連番** | sar の追加で既存イベントの連番がずれることを **A014 §4 が ★ACCEPTED★ として明示**している。CR-003 の体系は変えない | ○(判断済み) |

---

## 5. 次のフェーズへの引き継ぎ

* **P011** で 6 件の影響範囲を分析し、**P012** で是正する。
* **#5(17章のグラフ)は CR-009 の反映漏れであり、CR-010 のスコープ外である。**
  しかし**同じ文書内の矛盾**であり、放置すると後続の Agent が「チャートは対象外」と誤読する。
  `docs/P903-cr-records/CR-010.md` の「事前のスコープ想定と実際の差異」に記録する。

---

## 6. 再実行(2 回目)の結果 — 2026-09-08

**P012 による是正後、6 件をすべて再確認した。残存 0 件。合格。**

| # | 是正した箇所 | 確認 |
| --- | --- | --- |
| 1 | `docs/P002-frontend-spec.md` 2.2 の「違反時」列 → 「警告(未知の語)/ 終了コード 3(値そのものが読み取れない場合)」 | P003 DS-02-05・P001 FR-149 と一致 |
| 2 | `docs/P007-impl-direction/U010-sar.md` §4.5 に `await` の注意書き | 追加済み |
| 3 | `docs/P006-test-plan.md` 5.3 に A-12 / A-13 を補記 | A-01〜A-14 が連番で揃った |
| 4 | `docs/P005-impl-plan.md` 3.1 に U009 の脚注 | 表の再構成はしていない |
| 5 | `docs/P001-requirement.md` 17章のグラフの行 | 打ち消し線 + CR-009 で対象になった旨。13.2 と矛盾しなくなった |
| 6 | `docs/P003-backend-spec.md` 1.1 に `charts.py` | ツリーが実装と一致 |

**#5 の波及先の確認**: `docs/P004-traceability-matrix.md` 5章「対応する要求なし(過剰実装の疑い)」
の 2 件は、`vendor/` フォールバックと WARNING 文言であり、**チャートは含まれていない**。
FR-130〜136 として要求書に存在するため、過剰実装には当たらない。**P004 の修正は不要。**

**Plan Loop Step の終了条件を満たした。Overview Step(P020〜P022)へ進む。**
